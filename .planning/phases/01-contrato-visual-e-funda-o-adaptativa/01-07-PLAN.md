---
phase: 01-contrato-visual-e-funda-o-adaptativa
plan: "07"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/app/app_destinations.dart
  - lib/app/app_shell.dart
  - test/app/app_shell_test.dart
  - test/app/destination_catalog_test.dart
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-02
  - UI-06
  - UI-08
must_haves:
  truths:
    - "Com 5+ destinos, compacta <600 mostra 3 prioridades + Ferramentas; medium 600–839 e expanded >=840 listam TODOS os destinos na ordem do catálogo, em rail rolável, sem Ferramentas (D-01, D-02, D-03, CR-01)."
    - "Labels visíveis curtas Rede / Armazenamento / Hash; semanticLabel completo chega a tooltip na Bar e a tooltip/semantics no rail recolhido sem nós duplicados (D-16, CR-02, UI-06, UI-08)."
    - "appDestinations é List.unmodifiable; a seleção sobrevive a didUpdateWidget por id estável, sem índices inválidos (WR-03)."
    - "Testes em 599/600/839/840 e com 5 destinos em compact/medium/expanded inspecionam a árvore semântica, não só widgets visíveis."
  artifacts:
    - path: "lib/app/app_destinations.dart"
      provides: "Catálogo imutável com labels curtas e semanticLabel completo"
      contains: "List.unmodifiable"
    - path: "lib/app/app_shell.dart"
      provides: "Overflow só na NavigationBar; rail mapeia o catálogo inteiro"
      contains: "scrollable: true"
    - path: "test/app/app_shell_test.dart"
      provides: "Boundaries 599/600/839/840 e 5 destinos em compact/medium/expanded + semantics"
  key_links:
    - from: "lib/app/app_shell.dart"
      to: "lib/app/app_destinations.dart"
      via: "Rail usa widget.destinations na ordem do catálogo; Bar usa compactPriority só em overflow"
      pattern: "semanticLabel"
    - from: "lib/app/app_shell.dart"
      to: "NavigationDestination.tooltip / Tooltip do rail"
      via: "consumo de d.semanticLabel"
      pattern: "tooltip"
---

## Phase Goal

**As a** profissional de TI, **I want to** usar a fundação adaptativa e acessível, **so that** eu opere as ferramentas com clareza.

<objective>
Fechar o gap A do verifier e CR-01, CR-02, WR-03: navegação adaptativa honesta com 5 destinos, semanticLabel consumido, labels curtas do UI-SPEC e catálogo imutável com seleção por id.

Purpose: O usuário localiza qualquer destino em compacta, média e expandida, e o leitor de tela recebe o nome completo sem duplicar nós (per D-01, D-02, D-03, D-16).
Output: Catálogo imutável, shell corrigido, testes RED-então-GREEN que hoje falhariam contra o código atual.
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-VERIFICATION.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-REVIEW.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-CONTEXT.md
@lib/app/app_shell.dart
@lib/app/app_destinations.dart
@test/app/app_shell_test.dart
@test/app/destination_catalog_test.dart

<interfaces>
Estado atual que os testes novos devem fazer falhar:
- `_useOverflow` alimenta NavigationBar E NavigationRail; com 5 destinos o rail vira 3 + Ferramentas (CR-01, app_shell.dart ~232-248).
- `semanticLabel` nunca é lido no shell (CR-02).
- Labels visíveis: `Calculadora de Rede`, `Conversor de Dados`, `Gerador de Hash` — o UI-SPEC exige `Rede`, `Armazenamento`, `Hash`.
- `appDestinations` é `List` mutável; `didUpdateWidget` só chama `_buildPages()` (WR-03).

Contrato a implementar (UI-SPEC Navigation and Destination Catalog + Adaptive Layout):
- compact `< 600`: NavigationBar; 5+ destinos → 3 de menor compactPriority + quarta entrada `Ferramentas`.
- medium `>= 600 && < 840`: NavigationRail recolhido, `scrollable: true`, TODOS os destinos na ordem do catálogo, sem Ferramentas.
- expanded `>= 840`: NavigationRail extended, mesma lista completa.
- `NavigationDestination.tooltip` = `d.semanticLabel`. Rail recolhido: Tooltip no ícone com `d.semanticLabel`; não empilhar Tooltip + Semantics com o mesmo rótulo (sem nós duplicados). Rail extended: label visível curta; semanticLabel ainda anunciável uma vez.
- Catálogo inicial: id estável existente `network_calculator` / `data_converter` / `hash_generator` (não renomear ids); label `Rede` / `Armazenamento` / `Hash`; semanticLabel `Calculadora de Rede` / `Conversor de Dados` / `Gerador de Hash`.
- `appDestinations = List.unmodifiable([...])`.
- Seleção por `id`; em didUpdateWidget preservar widgets de página por id, materializar só ids novos, normalizar se o id desaparecer. Handlers de Bar e Rail separados: Bar usa índice visual (overflow); Rail usa índice real do catálogo. Ao cruzar breakpoint, o destino real permanece o mesmo id.

Títulos das screens de produção (ex.: AppBar `Calculadora de Rede`) não mudam. Não migrar as três telas para primitives (Phase 2).
</interfaces>

<dirty_worktree_guard>
Não editar theme, primitives, galeria, goldens, pubspec, `pubspec.lock-old`, nem screens além do que o catálogo já referencia. Não adicionar destinos de Diagnóstico. Não criar `test/goldens/`.
</dirty_worktree_guard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever testes que falham contra o overflow no rail, labels longas e semanticLabel morto</name>
  <files>test/app/app_shell_test.dart, test/app/destination_catalog_test.dart</files>
  <read_first>
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — Adaptive Layout Contract, Navigation and Destination Catalog
    - lib/app/app_shell.dart — `_useOverflow`, `_buildNavigationBar`, `_buildNavigationRail`
    - lib/app/app_destinations.dart — labels atuais
    - test/app/app_shell_test.dart — `_buildFiveDestinations`, happy path que toca `Conversor de Dados` / `Calculadora de Rede` na barra
  </read_first>
  <behavior>
    - Catálogo: labels exatamente Rede, Armazenamento, Hash; semanticLabels exatamente Calculadora de Rede, Conversor de Dados, Gerador de Hash; `appDestinations.add` lança (lista unmodifiable).
    - 599 px + 5 destinos: NavigationBar com 3 pins + Ferramentas; sem NavigationRail.
    - 600 px e 839 px + 5 destinos: NavigationRail com 5 destinos na ordem do catálogo; `find.text('Ferramentas')` ausente no rail; `extended: false`.
    - 840 px + 5 destinos: rail extended com 5 destinos; sem Ferramentas.
    - Árvore semântica / tooltip do AppShell real contém os semanticLabels completos (não basta o campo no modelo).
    - didUpdateWidget: crescer de 3 para 5 destinos, reduzir, reordenar; seleção segue id; IndexedStack não recebe índice inválido.
    - Redimensionar de compacta (destino não pinado via Ferramentas) para 600/840 seleciona o destino real no rail, não o índice visual 3.
    - Happy path de produção: tocar `Armazenamento` / `Rede` na barra (labels curtas); AppBar da calculadora continua `Calculadora de Rede`.
  </behavior>
  <action>
Inverter `destination_catalog_test.dart` para esperar as três labels curtas e os três semanticLabels completos, mais um teste de imutabilidade. Estender `app_shell_test.dart` com os cenários de 5 destinos em 599/600/839/840, inspeção de semantics/tooltip (`tester.getSemantics` / `find.byTooltip`), e didUpdateWidget por id. Atualizar o happy path que hoje faz `tap(find.text('Conversor de Dados'))` / `tap(find.text('Calculadora de Rede'))` para as labels curtas da barra. Não importar mapas internos do shell. Rodar os testes novos e confirmar RED contra o código atual (rail ainda aplica overflow; labels ainda longas; semanticLabel não flui).
  </action>
  <acceptance_criteria>
    - Os testes novos falham pelas razões do gap A / CR-01 / CR-02 / WR-03, não por compile error de fixture.
    - Nenhum valor esperado é lido de `_useOverflow` ou de um helper de produção que espelhe o bug.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub test/app/destination_catalog_test.dart test/app/app_shell_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before implementation' }</automated>
  </verify>
  <done>A suíte de navegação está vermelha nos pontos que o verifier apontou; o happy path espera labels curtas na barra.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir catálogo imutável e shell (overflow só na Bar, rail completo, semanticLabel)</name>
  <files>lib/app/app_destinations.dart, lib/app/app_shell.dart, test/app/app_shell_test.dart</files>
  <read_first>
    - lib/app/app_shell.dart — `_buildNavigationRail`, `_onDestinationSelected`, `didUpdateWidget`
    - lib/app/app_destinations.dart — `appDestinations`
    - test/app/app_shell_test.dart — testes da Task 1
  </read_first>
  <action>
Em `app_destinations.dart`, trocar labels visíveis para Rede / Armazenamento / Hash e semanticLabels para os nomes completos; wrapping com `List.unmodifiable`. Não mudar ids. Em `app_shell.dart`, restringir Ferramentas/overflow a `_buildNavigationBar` quando length > 4 (per D-03). `_buildNavigationRail` mapeia `widget.destinations` na ordem original, `scrollable: true`, `selectedIndex` = índice real do catálogo, `onDestinationSelected` separado que seleciona por índice real. Consumir `d.semanticLabel`: `tooltip` em cada `NavigationDestination`; no rail recolhido, Tooltip no ícone com a mesma string, sem Semantics extra que duplique o anúncio. Guardar `_selectedId` (string do AppDestination.id); derivar índices visuais da Bar a partir dele; em `didUpdateWidget`, reconciliar páginas por id, materializar só destinos novos, se o id sumir cair no primeiro destino restante, e recalcular overflow. Títulos das screens intactos. Fazer os testes da Task 1 passarem.
  </action>
  <acceptance_criteria>
    - 5 destinos: Bar compacta = 3 + Ferramentas; rails 600/839/840 = 5 itens, ordem do catálogo, sem Ferramentas.
    - `semanticLabel` aparece na árvore semântica/tooltip; labels visíveis da barra/rail são curtas.
    - Catálogo imutável; seleção por id sobrevive a didUpdateWidget e a resize.
    - `flutter analyze --no-pub` limpo nos arquivos deste plano.
  </acceptance_criteria>
  <verify>
    <automated>dart format lib/app/app_destinations.dart lib/app/app_shell.dart test/app/app_shell_test.dart test/app/destination_catalog_test.dart
flutter test --no-pub test/app/destination_catalog_test.dart test/app/app_shell_test.dart
flutter analyze --no-pub lib/app/app_destinations.dart lib/app/app_shell.dart test/app/app_shell_test.dart test/app/destination_catalog_test.dart</automated>
  </verify>
  <done>O usuário vê todos os destinos no rail médio/expandido, overflow só em compacta, e o leitor de tela recebe o nome completo uma vez.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role | Owner |
|---|---|---|
| `lib/app/app_destinations.dart` | `appDestinations` unmodifiable; labels curtas | 01-07 |
| `lib/app/app_shell.dart` | overflow só na Bar; rail completo + semanticLabel | 01-07 |
| `test/app/app_shell_test.dart` | 5 destinos × 599/600/839/840 + semantics + didUpdateWidget | 01-07 |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Navegação adaptativa clara | 01-07 | COVERED | Gap A. |
| REQ | UI-01 | Abrir qualquer ferramenta compacta/larga | 01-07 | COVERED | Rail 5+. |
| REQ | UI-02 | Bar/Rail conforme espaço | 01-07 | COVERED | 599/600/839/840. |
| REQ | UI-06 | Rótulos semânticos | 01-07 | COVERED | semanticLabel consumido. |
| REQ | UI-08 | pt-BR | 01-07 | COVERED | Rede / Armazenamento / Hash. |
| CONTEXT | D-01 D-02 D-03 D-16 | Catálogo, Bar/Rail, overflow, 48×48/labels | 01-07 | COVERED | |
| REVIEW | CR-01 CR-02 WR-03 | | 01-07 | COVERED | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catálogo → chrome | Labels visíveis vs semanticLabel; overflow não pode esconder destinos largos. |
| Widget tests → TalkBack real | Semantics de teste não substituem o gate 01-11. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G07-01 | Information disclosure | semanticLabel vs label curta | mitigate | Nome completo só em tooltip/semantics; sem PII; catálogo estático. |
| T-01-G07-02 | Denial of service | 5+ destinos no rail | mitigate | `scrollable: true`; overflow compacto não reutilizado no rail. |
| T-01-G07-03 | Tampering | lista global `appDestinations` | mitigate | `List.unmodifiable`; seleção por id em didUpdateWidget. |
| T-01-G07-04 | Elevation of privilege | IndexedStack index | mitigate | Normalizar id ausente; nunca passar índice >= length. |
| T-01-SC | Tampering | packages | accept | Sem dependência nova. |
</threat_model>

<verification>
1. Testes de 5 destinos em 599/600/839/840 passam.
2. Catálogo imutável e labels curtas passam.
3. Semantics/tooltip expõem os três nomes completos no AppShell.
4. Analyzer limpo; nenhuma tela de produção migrada.
</verification>

<success_criteria>
- Gap A, CR-01, CR-02 e WR-03 fechados em código e testes que hoje seriam vermelhos.
- UI-01/UI-02/UI-06/UI-08 da fundação de navegação observáveis sem puxar Phase 2.
</success_criteria>

<output>
Create `.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-07-SUMMARY.md` when done
</output>
