---
phase: 01-contrato-visual-e-funda-o-adaptativa
plan: "08"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/theme/theme.dart
  - lib/design_system/tool_status_panel.dart
  - test/design_system/tool_components_test.dart
  - test/theme/theme_contract_test.dart
autonomous: true
gap_closure: true
requirements:
  - UI-04
  - UI-05
  - UI-08
must_haves:
  truths:
    - "Temas claro e escuro expõem os hexadecimais exatos do UI-SPEC para canvas, surface, textos, outline, accent, containers e error (D-05, D-08, WR-01, UI-04)."
    - "Card elevation 0 com borda neutra 1 px; AppBar/nav/rail elevation 0 e scrolledUnderElevation 0; botões radius 8; chips radius 4 (WR-01)."
    - "Os sete estados usam ícone/heading/body/cor/semântica canônicos do UI-SPEC, não o _statusMap atual (CR-04, UI-05, UI-08)."
    - "Testes de status e tema falhariam hoje: expectativas independentes do UI-SPEC, nunca copiadas de _statusMap nem de ColorScheme.fromSeed."
  artifacts:
    - path: "lib/theme/theme.dart"
      provides: "lightTheme/darkTheme com roles e shapes exatos"
      exports: ["lightTheme", "darkTheme"]
    - path: "lib/design_system/tool_status_panel.dart"
      provides: "Sete variantes canônicas"
      contains: "Nenhum resultado ainda"
    - path: "test/theme/theme_contract_test.dart"
      provides: "Asserts hex/elevation/radius independentes"
    - path: "test/design_system/tool_components_test.dart"
      provides: "Grupo status com copy canônica independente"
  key_links:
    - from: "lib/theme/theme.dart"
      to: "ColorScheme roles UI-SPEC"
      via: "copyWith/construção explícita após fromSeed"
      pattern: "0xFFF7F9F7"
    - from: "lib/design_system/tool_status_panel.dart"
      to: "UI-SPEC State Contract"
      via: "_statusMap substituído por valores canônicos"
      pattern: "Não foi possível concluir"
---

## Phase Goal

**As a** profissional de TI, **I want to** usar a fundação adaptativa e acessível, **so that** eu opere as ferramentas com clareza.

<objective>
Fechar gaps B/C (tema + sete estados) e CR-04/WR-01, mais a parte de G que é teste tautológico de status: implementar o contrato visual exato e inverter testes ANTES do GREEN.

Purpose: Hierarquia e estados deixam de ser uma aproximação de fromSeed/_statusMap e passam a ser o UI-SPEC (per D-05, D-07, D-08, D-12).
Output: Temas canônicos, ToolStatusPanel canônico, testes independentes em vermelho e depois verdes. Galeria pt-BR e goldens ficam no 01-10.
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-REVIEW.md
@lib/theme/theme.dart
@lib/design_system/tool_status_panel.dart
@lib/design_system/app_tokens.dart
@test/design_system/tool_components_test.dart

<interfaces>
Valores atuais que DEVEM fazer os testes novos falharem:
- canvas `#FBFDF8` / `#1A1C19`; Card elevation 1; AppBar scrolledUnderElevation 1; botão radius 12; chip radius 8; ColorScheme.fromSeed sem override dos roles.
- empty: Pronto / Insira os dados e execute a ação.
- loading: hourglass_empty / Processando... / Aguarde a conclusão.
- failure: Falha / Não foi possível concluir a operação.
- offline: cloud_off / Sem conexão / cor error.
- permissionDenied: ícone onSurface (deveria ser error).
- cancelled: Cancelado / A operação foi cancelada.

Roles exatos a aplicar nos DOIS temas (UI-SPEC Color; hex é a fonte, não fromSeed):

Light: canvas/scaffoldBackgroundColor `#F7F9F7`; surface `#FFFFFF`; surfaceContainer `#EDF3EE`; onSurface `#171D18`; onSurfaceVariant `#47534A`; outline `#707A72`; primary `#006D2C`; onPrimary `#FFFFFF`; primaryContainer `#B8F3C5`; onPrimaryContainer `#002109`; error `#BA1A1A`; onError `#FFFFFF`.

Dark: canvas `#0F1511`; surface `#151C17`; surfaceContainer `#1D2820`; onSurface `#E1E9E2`; onSurfaceVariant `#BBC5BC`; outline `#859087`; primary `#50FA7B`; onPrimary `#003913`; primaryContainer `#005321`; onPrimaryContainer `#A7F5B7`; error `#FFB4AB`; onError `#690005`.

Shapes/elevations (ambos os temas): Card elevation 0 + BorderSide outline 1 px + radius 12 (`AppTokens.radius12`); AppBar elevation 0 e scrolledUnderElevation 0; NavigationBar e NavigationRail elevation 0; Elevated/Text/OutlinedButton radius 8 (`AppTokens.radius8`); Chip radius 4 (`AppTokens.radius4`), sem elevation; SnackBar pode elevation 2 e radius 12 (overlay permitido). Não reintroduzir `background` / `onBackground` / `surfaceVariant`. Inputs dark não usam branco puro.

Sete estados (UI-SPEC State Contract) — copiar estas strings no TESTE como const local, nunca importar `_statusMap`:
- empty: `inbox_outlined` / `Nenhum resultado ainda` / `Preencha os campos e execute a ferramenta para ver os resultados.` / ícone onSurface
- loading: ProgressIndicator visível (indeterminado se `progress` for null; `Icons.progress_activity` só se existir no SDK, senão o indicador ocupa o slot de ícone) / `Processando` (sem reticências) / `Aguarde enquanto concluímos esta etapa.` / ícone/indicador onSurface
- success: `check_circle_outline` / `Concluído` / corpo default da primitive pode permanecer um resumo estático curto da fundação (não inventar dado de feature) / ícone primary
- failure: `error_outline` / `Não foi possível concluir` / `Confira os dados e tente novamente.` / ícone error
- offline: `wifi_off` / `Sem conexão com a internet` / `Verifique a conexão e tente novamente.` / ícone onSurface (neutro, NÃO error)
- permissionDenied: `lock_outline` / `Permissão necessária` / `Ative a permissão nas configurações para continuar.` / ícone error
- cancelled: `cancel_outlined` / `Operação cancelada` / `Os resultados concluídos continuam disponíveis.` / ícone onSurface

Não editar `design_system_gallery.dart` neste plano (01-10). Não regenerar goldens.
</interfaces>

<dirty_worktree_guard>
Não editar app_shell, app_destinations, copy_value_action, galeria, goldens, pubspec, `pubspec.lock-old`. Cirurgia em `tool_components_test.dart` somente no grupo `status` (ícone/heading/body e cor do ícone). Não reescrever testes de ToolScaffold/ações. Não migrar screens.
</dirty_worktree_guard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inverter testes de tema e dos sete estados com expectativas do UI-SPEC</name>
  <files>test/theme/theme_contract_test.dart, test/design_system/tool_components_test.dart</files>
  <read_first>
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — Color, Radius and Shape, State Contract
    - lib/theme/theme.dart — fromSeed, canvases, Card/AppBar/button/chip
    - lib/design_system/tool_status_panel.dart — `_statusMap` e `_iconColor`
    - test/design_system/tool_components_test.dart — grupo `status` com Pronto/Falha/etc.
  </read_first>
  <behavior>
    - lightTheme.scaffoldBackgroundColor == Color(0xFFF7F9F7); darkTheme == Color(0xFF0F1511).
    - Cada role listado em interfaces casa com o hex do UI-SPEC em claro e escuro (`value == expected.value`, não `isNotNull`).
    - CardTheme elevation 0; shape com radius 12 e side width 1 na cor outline.
    - AppBarTheme elevation 0 e scrolledUnderElevation 0 nos dois temas.
    - NavigationBarTheme/NavigationRailTheme elevation 0 quando o campo existir.
    - ElevatedButton/OutlinedButton/TextButton shape radius 8; Chip shape radius 4.
    - Grupo status: para cada variant, find.text e find.byIcon (ou find.byType(ProgressIndicator) no loading) usam a tabela canônica acima; permissionDenied e failure usam colorScheme.error no ícone; offline NÃO usa error.
  </behavior>
  <action>
Criar `test/theme/theme_contract_test.dart` com Color e int literais copiados do UI-SPEC (não de `lightTheme` atual como oracle). Substituir o mapa `variantData` em `tool_components_test.dart` pelas tuplas canônicas; adicionar asserts de cor do ícone via o Icon ou o Theme do painel. Não ler `_statusMap`. Rodar os dois arquivos e confirmar RED: canvas ainda FBFDF8, empty ainda Pronto, offline ainda cloud_off/Sem conexão, etc.
  </action>
  <acceptance_criteria>
    - `flutter test --no-pub test/theme/theme_contract_test.dart test/design_system/tool_components_test.dart` falha em asserts de hex/copy, não em compile.
    - Nenhuma expectativa é `isNotNull` para provar um role de cor.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub test/theme/theme_contract_test.dart test/design_system/tool_components_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before implementation' }</automated>
  </verify>
  <done>Os testes documentam o contrato canônico e estão vermelhos contra a implementação atual.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aplicar ColorScheme/shapes exatos e os sete estados canônicos</name>
  <files>lib/theme/theme.dart, lib/design_system/tool_status_panel.dart</files>
  <read_first>
    - lib/theme/theme.dart — `_applyComponentThemes`, lightTheme, darkTheme
    - lib/design_system/app_tokens.dart — radius4/radius8/radius12
    - lib/design_system/tool_status_panel.dart — `_statusMap`, `_iconColor`
  </read_first>
  <action>
Em `theme.dart`, partir de `ColorScheme.fromSeed` se útil, mas sobrescrever TODOS os roles listados com os Color(0xFF...) do UI-SPEC e `scaffoldBackgroundColor` dos canvases. Card: elevation 0, shape radius 12, side 1 px outline. AppBar: elevation 0, scrolledUnderElevation 0, fundo surface. Nav bar/rail: elevation 0, indicador primaryContainer. Botões: radius 8 via tokens, minSize 48. Chip: radius 4. Não colocar hex em widgets consumidores. Em `tool_status_panel.dart`, substituir `_statusMap` e `_iconColor` pela tabela canônica (offline neutro; permissionDenied error; loading com ProgressIndicator e heading `Processando`). PreservedChild continua em loading/failure/cancelled. Fazer os testes da Task 1 passarem nos dois temas.
  </action>
  <acceptance_criteria>
    - theme_contract_test e o grupo status passam em light e dark.
    - Nenhuma string Pronto, Processando..., Falha, Sem conexão (curta), Cancelado isolado permanece em produção.
    - Analyzer limpo nos arquivos deste plano.
  </acceptance_criteria>
  <verify>
    <automated>dart format lib/theme/theme.dart lib/design_system/tool_status_panel.dart test/theme/theme_contract_test.dart test/design_system/tool_components_test.dart
flutter test --no-pub test/theme/theme_contract_test.dart test/design_system/tool_components_test.dart
flutter analyze --no-pub lib/theme/theme.dart lib/design_system/tool_status_panel.dart test/theme/theme_contract_test.dart test/design_system/tool_components_test.dart</automated>
  </verify>
  <done>Tema e sete estados coincidem com o UI-SPEC; a suíte deixa de ser tautológica nesses pontos.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role | Owner |
|---|---|---|
| `lib/theme/theme.dart` | Roles/shapes/elevations canônicos | 01-08 |
| `lib/design_system/tool_status_panel.dart` | Copy/ícone/cor canônicos | 01-08 |
| `test/theme/theme_contract_test.dart` | Asserts hex independentes | 01-08 |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | UI-04 | Claro/escuro sem perda | 01-08 | COVERED | Hex exatos. |
| REQ | UI-05 | Sete estados | 01-08 | COVERED | CR-04. |
| REQ | UI-08 | pt-BR | 01-08 | COVERED | Copy canônica dos estados. |
| CONTEXT | D-05 D-07 D-08 D-12 | Accent, hierarquia, roles, estados honestos | 01-08 | COVERED | |
| REVIEW | CR-04 WR-01 WR-05 (status tautológico) | | 01-08 | COVERED | Galeria/a11y/goldens → 01-10. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-SPEC → ThemeData | fromSeed não é oracle; hex do contrato é. |
| Status copy → usuário | Strings erradas enganam diagnóstico; devem ser as do UI-SPEC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G08-01 | Spoofing | ToolStatusPanel | mitigate | Ícone+heading+body canônicos; cor nunca único diferenciador; offline ≠ failure. |
| T-01-G08-02 | Information disclosure | ColorScheme | accept | Sem PII; paleta local. |
| T-01-G08-03 | Tampering | testes de status | mitigate | Expectativas literais do UI-SPEC no arquivo de teste, não `_statusMap`. |
| T-01-SC | Tampering | packages | accept | Sem dependência; sem google_fonts. |
</threat_model>

<verification>
1. Testes de tema hex/elevation/radius passam.
2. Sete estados canônicos passam em light e dark.
3. Galeria e goldens intocados neste plano.
</verification>

<success_criteria>
- Gaps B/C (exceto copy da galeria) e CR-04/WR-01 fechados.
- Testes independentes teriam falhado no HEAD atual.
</success_criteria>

<output>
Create `.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-08-SUMMARY.md` when done
</output>
