---
phase: 01-contrato-visual-e-funda-o-adaptativa
plan: "10"
type: execute
wave: 3
depends_on:
  - 01-07
  - 01-09
files_modified:
  - lib/design_system/tool_sections.dart
  - test/design_system/tool_metric_reflow_test.dart
  - test/design_system/accessibility_test.dart
  - test/design_system/design_system_gallery.dart
  - test/design_system/design_system_golden_test.dart
  - test/manual/design_system_app.dart
  - test/design_system/goldens/primitives_compact_light.png
  - test/design_system/goldens/primitives_compact_dark.png
  - test/design_system/goldens/shell_medium_light.png
  - test/design_system/goldens/shell_expanded_dark.png
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-03
  - UI-06
  - UI-07
  - UI-08
  - UI-09
must_haves:
  truths:
    - "ToolMetric real reflui em 360 px com text scaler 2.0; o Wrap externo deixa de ser a única defesa (WR-04, UI-07)."
    - "accessibility_test.dart exercita widgets públicos reais: AppShell, ToolScaffold, ToolMetric, ToolStatusPanel, TechnicalValueRow com copy, cinco destinos e semantics — sem fixture que clone as regras (WR-05)."
    - "Galeria/harness mostram a ação de copiar e copy pt-BR canônica: Calcular rede, Painéis de status, título pt-BR (WR-06, UI-08, UI-09)."
    - "Caminho canônico dos goldens é test/design_system/goldens/ (override formal de 01-04-PLAN). Fixtures largas usam 5 destinos e copy visível; PNGs regenerados só depois da implementação."
  artifacts:
    - path: "lib/design_system/tool_sections.dart"
      provides: "ToolMetric flexível/refluível"
    - path: "test/design_system/accessibility_test.dart"
      provides: "Matriz sobre widgets públicos reais"
      contains: "AppShell"
    - path: "test/design_system/design_system_gallery.dart"
      provides: "Galeria com copy e copy pt-BR"
      contains: "Calcular rede"
    - path: "test/design_system/design_system_golden_test.dart"
      provides: "matchesGoldenFile('goldens/....png') → test/design_system/goldens/"
      contains: "goldens/"
  key_links:
    - from: "test/design_system/design_system_golden_test.dart"
      to: "test/design_system/goldens"
      via: "matchesGoldenFile relativo ao arquivo de teste"
      pattern: "matchesGoldenFile\\('goldens/"
    - from: "test/design_system/design_system_gallery.dart"
      to: "TechnicalValueRow.copyWriter"
      via: "ação de copiar renderizada"
      pattern: "copyWriter"
---

## Phase Goal

**As a** profissional de TI, **I want to** usar a fundação adaptativa e acessível, **so that** eu opere as ferramentas com clareza.

<objective>
Fechar gaps E/F e o restante de G (WR-04, WR-05, WR-06): reflow de ToolMetric, matriz de a11y real, galeria pt-BR com cópia, e override formal + alargamento dos goldens.

Purpose: A evidência automatizada passa a proteger os blockers atuais em vez de uma réplica que os evita (per D-17, D-18, UI-03 da fundação compartilhada).
Output: ToolMetric refluível, testes reais, galeria/harness canônicos, quatro PNGs em `test/design_system/goldens/`. Aprovação visual humana é 01-11, não este plano.

Nota UI-03: este plano entrega hierarquia/primitives/galeria da fundação. Não migrar as três telas de produção (Phase 2).
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-04-PLAN.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-07-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-08-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-09-SUMMARY.md
@lib/design_system/tool_sections.dart
@test/design_system/accessibility_test.dart
@test/design_system/design_system_gallery.dart
@test/design_system/design_system_golden_test.dart
@test/manual/design_system_app.dart

<interfaces>
Override formal do caminho golden (01-04-PLAN frontmatter citava `test/goldens/`, que não existe):
- `matchesGoldenFile('goldens/primitives_compact_light.png')` (e os outros três nomes) declarado em `test/design_system/design_system_golden_test.dart` resolve para `test/design_system/goldens/`.
- Essa é a única localização canônica. NÃO criar `test/goldens/`. NÃO duplicar PNGs.

Bug ToolMetric (tool_sections.dart ~99-117): `Row(mainAxisSize: min)` com dois `Text` não flexíveis. Wrap em `ToolMetricLayout` só empurra o chip inteiro. Em 360 px + TextScaler.linear(2.0) com label longa e valor `Indisponível`, deve refluir (Flexible/Wrap/coluna) sem overflow horizontal.

A11y atual: `_buildFixture` / `_FakeDestination` (accessibility_test.dart ~8-95, 318-347) não monta AppShell/ToolScaffold/ToolMetric reais e não usa semanticLabel.

Galeria atual: TechnicalValueRow sem copyWriter; CTA `Calcular`; heading `Status Panels`; harness `title: 'Design System Gallery'`.

Contrato da galeria (cirúrgico, UI-SPEC Copywriting):
- CTA primária: `Calcular rede` (verbo + objeto; `Calcular` isolado é proibido na fundação/galeria).
- Heading da vitrine de estados: `Painéis de status`.
- Harness `MaterialApp.title`: `Galeria do sistema de design`.
- `TechnicalValueRow(label: 'Hash SHA-256', value: estático, copyWriter: NoopCopyWriter)` para a ação aparecer.
- Não traduzir às cegas outros textos já em pt-BR (`Galeria de Componentes`, `Endereço IP`, `Limpar`, `Indisponível`).

Goldens a alargar (mesmos 4 arquivos, fixtures mais ricas):
- compact light/dark 360×800: galeria com copy visível e sete estados.
- shell medium light 720×1024: AppShell com 5 destinos, rail recolhido, 5 labels curtas, sem Ferramentas.
- shell expanded dark 1024×768: AppShell com 5 destinos, rail extended, copy no destino galeria se a página 0 for a gallery.

Não tratar PNG regenerado como visualmente correto — 01-11 faz a revisão humana.
</interfaces>

<dirty_worktree_guard>
Não criar `test/goldens/`. Não deletar `pubspec.lock-old`. Não migrar `lib/screen/*`. Não adicionar pacotes. Regenerar PNGs somente na última task, depois de reflow/galeria/a11y implementados. `depends_on` 01-07 e 01-09: usar labels curtas, rail completo e `copyWriter`.
</dirty_worktree_guard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes reais de reflow, a11y pública e fixtures golden que falham hoje</name>
  <files>test/design_system/tool_metric_reflow_test.dart, test/design_system/accessibility_test.dart, test/design_system/design_system_golden_test.dart</files>
  <read_first>
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — Typography, Adaptive Layout, Widget and Golden Verification
    - lib/design_system/tool_sections.dart — ToolMetric Row
    - test/design_system/accessibility_test.dart — `_buildFixture`
    - test/design_system/design_system_golden_test.dart — `_fakeDestinations` de 3 itens
  </read_first>
  <behavior>
    - ToolMetric com label longa (≥40 caracteres) + valor null (`Indisponível`) em Surface 360×800, TextScaler.linear(2.0): zero FlutterError de overflow; conteúdo alcançável por scroll vertical se o pai rolar.
    - Matriz 360/720/1024 × scaler 1.0/2.0 monta AppShell real (catálogo de 5 AppDestination, um deles com DesignSystemGallery) + ToolScaffold/ToolMetric/ToolStatusPanel/TechnicalValueRow visíveis; androidTapTargetGuideline, labeledTapTargetGuideline, textContrastGuideline em claro e escuro.
    - Semantics do AppShell incluem os semanticLabel completos; Bar em 360 com 5 destinos mostra Ferramentas; rail em 720 e 1024 mostra 5 destinos e não mostra Ferramentas.
    - Golden test file continua usando `matchesGoldenFile('goldens/....png')`; fixture medium/expanded passa a ter 5 destinos. Comentário no arquivo declara o override: caminho canônico = `test/design_system/goldens/`.
  </behavior>
  <action>
Criar `tool_metric_reflow_test.dart` exercitando a primitive pública, não uma Row clone. Substituir `_buildFixture` em `accessibility_test.dart` por AppShell + DesignSystemGallery (ou composição equivalente dos widgets públicos listados). Remover a classe `_FakeDestination` que não é `AppDestination`. Estender `_fakeDestinations` dos goldens para 5 entradas na ordem de catálogo, labels curtas, semanticLabels completos; páginas 0 = gallery. Documentar o override de caminho no comentário de cabeçalho. Não atualizar PNGs nesta task. Rodar os testes novos e confirmar RED (overflow de ToolMetric e/ou fixture antiga ainda presente / goldens divergentes pela fixture de 5 destinos).
  </action>
  <acceptance_criteria>
    - tool_metric_reflow_test falha no HEAD pré-reflow.
    - accessibility_test importa e monta AppShell, ToolMetric e TechnicalValueRow; não resta `_buildFixture` como shell substituto.
    - Nenhum arquivo criado em `test/goldens/`.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub test/design_system/tool_metric_reflow_test.dart test/design_system/accessibility_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before implementation' }</automated>
  </verify>
  <done>A evidência automatizada aponta para os widgets reais e para o reflow que hoje quebra.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reflow de ToolMetric e galeria/harness pt-BR com copy visível</name>
  <files>lib/design_system/tool_sections.dart, test/design_system/design_system_gallery.dart, test/manual/design_system_app.dart</files>
  <read_first>
    - lib/design_system/tool_sections.dart — ToolMetric, TechnicalValueRow
    - test/design_system/design_system_gallery.dart — Calcular, Status Panels, TechnicalValueRow
    - test/manual/design_system_app.dart — title inglês
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-09-SUMMARY.md — contrato copyWriter
  </read_first>
  <action>
Alterar ToolMetric para que label (sans) e valor (mono, `Indisponível` se null) possam quebrar/refluir: Flexible/Wrap ou coluna quando a largura intrínseca não couber; manter gap 8; não usar maxLines:1 nem ellipsis em informação essencial. Na galeria, injetar um `CopyValueWriter` no-op que completa imediatamente; passar `copyWriter` em TechnicalValueRow; trocar o filho do primary de `Calcular` para `Calcular rede`; trocar `Status Panels` para `Painéis de status`. No harness, `title: 'Galeria do sistema de design'`. Não alterar CTAs das screens de produção. Fazer tool_metric_reflow_test e a matriz de a11y passarem (exceto goldens, que ainda podem divergir até a Task 3).
  </action>
  <acceptance_criteria>
    - tool_metric_reflow_test verde.
    - Galeria contém `Calcular rede`, `Painéis de status` e um `CopyValueAction` visível.
    - Harness title em pt-BR.
    - `Calcular` isolado e `Status Panels` não restam na galeria/harness.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub test/design_system/tool_metric_reflow_test.dart test/design_system/accessibility_test.dart test/design_system/tool_components_test.dart</automated>
  </verify>
  <done>Reflow e galeria canônica estão no código; goldens ainda não foram regenerados.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerar os quatro goldens canônicos depois da implementação</name>
  <files>test/design_system/goldens/primitives_compact_light.png, test/design_system/goldens/primitives_compact_dark.png, test/design_system/goldens/shell_medium_light.png, test/design_system/goldens/shell_expanded_dark.png, test/design_system/design_system_golden_test.dart</files>
  <read_first>
    - test/design_system/design_system_golden_test.dart — nomes e viewports
    - test/design_system/goldens/ — local canônico
  </read_first>
  <action>
Com reflow, tema canônico (01-08), copy visível e 5 destinos no rail já no código, atualizar as baselines SOMENTE em `test/design_system/goldens/` via `flutter test --no-pub --update-goldens test/design_system/design_system_golden_test.dart` no host Windows, Flutter 3.44.x, DPR 1, sem rede/fonte remota. Confirmar que os quatro arquivos existem nesse diretório e que `test/goldens/` continua ausente. Não declarar aprovação visual. Comentário no golden test reitera o override de 01-04-PLAN.
  </action>
  <acceptance_criteria>
    - Quatro PNGs atualizados em `test/design_system/goldens/` após a implementação.
    - `flutter test --no-pub test/design_system/design_system_golden_test.dart` passa.
    - `test/goldens/` não foi criado.
    - Nenhuma afirmação de que o PNG está visualmente aprovado (isso é 01-11).
  </acceptance_criteria>
  <verify>
    <automated>dart format lib/design_system/tool_sections.dart test/design_system/tool_metric_reflow_test.dart test/design_system/accessibility_test.dart test/design_system/design_system_gallery.dart test/design_system/design_system_golden_test.dart test/manual/design_system_app.dart
flutter test --no-pub test/design_system/design_system_golden_test.dart
python -c "from pathlib import Path; root=Path('test'); assert not (root/'goldens').exists() or not any((root/'goldens').glob('*.png')), 'do not create test/goldens'; d=root/'design_system'/'goldens';
for n in ['primitives_compact_light.png','primitives_compact_dark.png','shell_medium_light.png','shell_expanded_dark.png']:
 assert (d/n).is_file(), n
print('canonical goldens present')"</automated>
  </verify>
  <done>Goldens canônicos existem só em test/design_system/goldens/ e cobrem 5 destinos + copy; revisão humana fica para 01-11.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role | Owner |
|---|---|---|
| `lib/design_system/tool_sections.dart` | ToolMetric refluível | 01-10 |
| `test/design_system/tool_metric_reflow_test.dart` | Caso 360×2.0 | 01-10 |
| `test/design_system/accessibility_test.dart` | Matriz de widgets públicos | 01-10 |
| `test/design_system/design_system_gallery.dart` | Copy + pt-BR canônico | 01-10 |
| `test/design_system/goldens/*.png` | Quatro baselines alargadas | 01-10 |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | UI-03 | Hierarquia compartilhada | 01-10 | COVERED | Galeria completa; screens → Phase 2. |
| REQ | UI-07 | Fonte ampliada | 01-10 | COVERED | ToolMetric 360/2.0. |
| REQ | UI-08 UI-09 | pt-BR + copy visível | 01-10 | COVERED | Galeria/harness. |
| REQ | UI-01 UI-06 | Rail 5 dest + semantics na matriz | 01-10 | COVERED | Depende de 01-07. |
| CONTEXT | D-17 D-18 | Reflow; goldens enxutos | 01-10 | COVERED | Override de path. |
| REVIEW | WR-04 WR-05 WR-06 | | 01-10 | COVERED | Status tautológico já em 01-08. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture de teste → produção | Galeria/harness não são destinos de produto. |
| PNG atualizado → verdade visual | Update-goldens não substitui revisão humana (01-11). |
| Clipboard no golden | Writer no-op; sem clipboard real, sem rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G10-01 | Spoofing | accessibility fixture | mitigate | Montar AppShell/ToolScaffold/primitives públicos; proibir clone das regras. |
| T-01-G10-02 | Tampering | golden path | mitigate | Override explícito para test/design_system/goldens/; proibir test/goldens/. |
| T-01-G10-03 | Information disclosure | gallery copy | accept | Valor estático de fixture; writer no-op; sem telemetria. |
| T-01-G10-04 | Denial of service | ToolMetric overflow | mitigate | Reflow interno; teste 360/2.0. |
| T-01-SC | Tampering | packages | accept | Sem install. |
</threat_model>

<verification>
1. tool_metric_reflow_test e accessibility_test passam sobre widgets reais.
2. Galeria tem copy + Calcular rede + Painéis de status.
3. Quatro goldens em test/design_system/goldens/; test/goldens/ ausente.
4. `flutter analyze --no-pub` e a suíte relevante passam; PNG não conta como aprovação visual.
</verification>

<success_criteria>
- Gaps E/F e WR-04/WR-05/WR-06 fechados na evidência automatizada.
- Override de golden path está documentado no teste e no SUMMARY.
- Nenhuma tela de produção migrada.
</success_criteria>

<output>
Create `.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-10-SUMMARY.md` when done
</output>
