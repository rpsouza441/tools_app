---
phase: 01-contrato-visual-e-funda-o-adaptativa
plan: "09"
type: tdd
wave: 2
depends_on:
  - 01-08
files_modified:
  - lib/design_system/copy_value_action.dart
  - lib/design_system/tool_sections.dart
  - test/design_system/tool_components_test.dart
  - test/design_system/copy_lifecycle_test.dart
autonomous: true
gap_closure: true
requirements:
  - UI-06
  - UI-09
must_haves:
  truths:
    - "Após await, CopyValueAction só toca SnackBar se context.mounted; fecha somente o ScaffoldFeatureController próprio; nunca hideCurrentSnackBar global (CR-03, D-13, UI-09)."
    - "TechnicalValueRow expõe CopyValueWriter? copyWriter que é de fato passado a CopyValueAction e exercitado no teste (WR-02)."
    - "Dispose antes da conclusão do writer não lança; SnackBar alheio permanece; semantics Copiar {rótulo}; confirmação {Rótulo} copiado."
  artifacts:
    - path: "lib/design_system/copy_value_action.dart"
      provides: "StatefulWidget com controller próprio e guarda mounted"
      contains: "ScaffoldFeatureController"
    - path: "lib/design_system/tool_sections.dart"
      provides: "TechnicalValueRow.copyWriter conectado"
      contains: "copyWriter"
    - path: "test/design_system/copy_lifecycle_test.dart"
      provides: "dispose-before-complete, SnackBar alheio, writer exercitado"
  key_links:
    - from: "lib/design_system/tool_sections.dart"
      to: "lib/design_system/copy_value_action.dart"
      via: "CopyValueAction(writer: copyWriter)"
      pattern: "CopyValueAction\\("
    - from: "CopyValueAction"
      to: "ScaffoldMessenger"
      via: "ScaffoldFeatureController próprio; close() sem hideCurrentSnackBar"
      pattern: "ScaffoldFeatureController"
---

## Phase Goal

**As a** profissional de TI, **I want to** usar a fundação adaptativa e acessível, **so that** eu opere as ferramentas com clareza.

<objective>
Fechar o gap D, CR-03 e WR-02: cópia segura no lifecycle, ownership do SnackBar e API pública real de TechnicalValueRow.

Purpose: Copiar valor técnico não derruba a árvore, não apaga avisos alheios e realmente invoca o writer do consumidor (per D-13, UI-09).
Output: CopyValueAction stateful, copyWriter conectado, testes que hoje falhariam. Não regenerar goldens; a galeria consome esta API no 01-10.
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-REVIEW.md
@lib/design_system/copy_value_action.dart
@lib/design_system/tool_sections.dart
@test/design_system/tool_components_test.dart

<interfaces>
Bugs atuais:
- `CopyValueAction` é StatelessWidget; `_copy` captura `ScaffoldMessengerState`, dá await, chama `hideCurrentSnackBar()` e `showSnackBar` sem `context.mounted` (copy_value_action.dart ~59-81).
- `TechnicalValueRow.onCopy` é `VoidCallback?` usado só como booleano; nunca invocado (tool_sections.dart ~158-160, 200).

API pública a adotar (discrição do executor, mas o teste deve provar invocação real):
- Remover `onCopy`.
- `TechnicalValueRow({ required label, required value, metadata, CopyValueWriter? copyWriter })`.
- Se `copyWriter != null`, construir `CopyValueAction(label: label, value: value, writer: copyWriter)`.
- `CopyValueAction` vira StatefulWidget. Após `await writer.write`, se `!context.mounted` return. Guardar `ScaffoldFeatureController<SnackBar>? _copySnackBar`; antes de um novo feedback de cópia, `_copySnackBar?.close()` somente; nunca `hideCurrentSnackBar`. Sucesso: SnackBar 2s com `{Rótulo} copiado` (primeira letra maiúscula). Falha: `Não foi possível copiar. Selecione o valor e copie manualmente.` Tooltip/semantics: `Copiar {rótulo}` (UI-SPEC Copy Interaction). Sem `SemanticsService.announce` paralelo.

Contrato para 01-10: a galeria passará `copyWriter:` de um no-op/fake. Não editar a galeria neste plano.

`depends_on: 01-08` existe só por ownership de `tool_components_test.dart` (grupo status já invertido no 01-08). Não reverter as strings canônicas de status.
</interfaces>

<dirty_worktree_guard>
Não editar theme, app_shell, galeria, goldens, accessibility_test, pubspec, `pubspec.lock-old`. Em `tool_components_test.dart`, apenas migrar call sites `onCopy:` → `copyWriter:` e asserts de presença do botão; testes novos de lifecycle vão para `copy_lifecycle_test.dart`. Não migrar screens.
</dirty_worktree_guard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever testes de dispose, SnackBar alheio e writer realmente chamado</name>
  <files>test/design_system/copy_lifecycle_test.dart</files>
  <read_first>
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — Copy Interaction, Copywriting Contract
    - lib/design_system/copy_value_action.dart — `_copy`
    - lib/design_system/tool_sections.dart — `TechnicalValueRow`
    - test/design_system/tool_components_test.dart — usos de `onCopy: () {}`
  </read_first>
  <behavior>
    - Writer com Completer: pump CopyValueAction, tap, dispose da árvore, completar o Completer; tester.binding garante zero exceção (`FlutterError` / setState after dispose).
    - Mostrar um SnackBar `Aviso não relacionado` via ScaffoldMessenger, então copiar com writer síncrono; o texto do aviso permanece (não foi hideCurrentSnackBar).
    - TechnicalValueRow com RecordingCopyWriter: tap no IconButton/CopyValueAction; `writer.written == valor exato`; não basta `findsOneWidget` do botão.
    - Tooltip/semantics `Copiar endereço de rede` quando label é `endereço de rede`; SnackBar `Endereço de rede copiado`.
    - Falha do writer: SnackBar `Não foi possível copiar. Selecione o valor e copie manualmente.`
  </behavior>
  <action>
Criar somente `copy_lifecycle_test.dart` com fakes `CompleterCopyWriter` e `RecordingCopyWriter` implementando `CopyValueWriter`, usando a API alvo `copyWriter` em TechnicalValueRow. Não migrar `tool_components_test.dart` nesta task (isso é da Task 2, para não misturar o RED de lifecycle com o grupo status do 01-08). Rodar `copy_lifecycle_test.dart` e confirmar RED: compile contra `onCopy` e/ou falha comportamental (dispose explode, SnackBar some, writer da row não chamado).
  </action>
  <acceptance_criteria>
    - Pelo menos um teste de dispose, um de SnackBar alheio e um de RecordingCopyWriter via TechnicalValueRow existem e falham no HEAD atual.
    - Não há teste que apenas verifica `onCopy != null`.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub test/design_system/copy_lifecycle_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before implementation' }</automated>
  </verify>
  <done>Os gaps de lifecycle e API morta estão reproduzidos em vermelho.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tornar CopyValueAction seguro e ligar copyWriter em TechnicalValueRow</name>
  <files>lib/design_system/copy_value_action.dart, lib/design_system/tool_sections.dart, test/design_system/tool_components_test.dart</files>
  <read_first>
    - lib/design_system/copy_value_action.dart
    - lib/design_system/tool_sections.dart — TechnicalValueRow
    - test/design_system/copy_lifecycle_test.dart
  </read_first>
  <action>
Converter `CopyValueAction` em StatefulWidget. Depois de cada await, `if (!context.mounted) return`. Manter `ScaffoldFeatureController<SnackBar>?` do próprio `showSnackBar` e dar `close()` só nele antes do próximo feedback de cópia. Remover qualquer `hideCurrentSnackBar`. Trocar `TechnicalValueRow.onCopy` por `copyWriter` e passar `writer: copyWriter` para `CopyValueAction`. Atualizar os call sites restantes em `tool_components_test.dart`. Confirmar que `hideCurrentSnackBar` não aparece em `lib/design_system/copy_value_action.dart` fora de comentários. Fazer `copy_lifecycle_test.dart` e o grupo TechnicalValueRow passarem.
  </action>
  <acceptance_criteria>
    - copy_lifecycle_test verde: dispose silencioso, SnackBar alheio visível, writer da row chamado com o valor.
    - Produção não contém `hideCurrentSnackBar` em copy_value_action.dart.
    - Sem `VoidCallback? onCopy` em TechnicalValueRow.
  </acceptance_criteria>
  <verify>
    <automated>dart format lib/design_system/copy_value_action.dart lib/design_system/tool_sections.dart test/design_system/copy_lifecycle_test.dart test/design_system/tool_components_test.dart
flutter test --no-pub test/design_system/copy_lifecycle_test.dart test/design_system/tool_components_test.dart
flutter analyze --no-pub lib/design_system/copy_value_action.dart lib/design_system/tool_sections.dart test/design_system/copy_lifecycle_test.dart test/design_system/tool_components_test.dart</automated>
  </verify>
  <done>Cópia é segura no dispose, não apaga avisos alheios e a API pública da row é exercitada.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role | Owner |
|---|---|---|
| `lib/design_system/copy_value_action.dart` | Stateful CopyValueAction + controller | 01-09 |
| `lib/design_system/tool_sections.dart` | `TechnicalValueRow.copyWriter` | 01-09 |
| `test/design_system/copy_lifecycle_test.dart` | Testes de lifecycle/ownership | 01-09 |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | UI-09 | Cópia com confirmação acessível | 01-09 | COVERED | Lifecycle + SnackBar próprio. |
| REQ | UI-06 | Semantics Copiar {rótulo} | 01-09 | COVERED | |
| CONTEXT | D-13 | Feedback anunciável; sem UI após dispose | 01-09 | COVERED | |
| REVIEW | CR-03 WR-02 | | 01-09 | COVERED | Galeria/copy visível → 01-10. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clipboard (SO) → app | Valor copiado é exatamente o exibido; writer injetável em teste. |
| Copy SnackBar → outros SnackBars | Ownership do controller; não limpar a fila global. |
| async write → widget tree | mounted após await. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G09-01 | Tampering | clipboard | mitigate | Copia só `widget.value` visível; sem prefixos; writer injetável. |
| T-01-G09-02 | Denial of service | ScaffoldMessenger | mitigate | Fechar só o controller de cópia; preservar SnackBars alheios. |
| T-01-G09-03 | Elevation of privilege | setState após dispose | mitigate | `context.mounted` após await; abandonar conclusão tardia. |
| T-01-G09-04 | Information disclosure | clipboard / SnackBar | accept | Valores técnicos da sessão; sem telemetria; sem permissão nova. |
| T-01-SC | Tampering | packages | accept | Sem dependência nova. |
</threat_model>

<verification>
1. copy_lifecycle_test cobre dispose, SnackBar alheio e writer da row.
2. `rg hideCurrentSnackBar lib/design_system/copy_value_action.dart` não encontra o símbolo em código.
3. Galeria/goldens intocados.
</verification>

<success_criteria>
- Gap D / CR-03 / WR-02 fechados em código testável.
- Contrato `copyWriter` pronto para a galeria do 01-10.
</success_criteria>

<output>
Create `.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-09-SUMMARY.md` when done
</output>
