---
phase: 01-contrato-visual-e-funda-o-adaptativa
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/app/app_destinations.dart
  - lib/app/app_shell.dart
  - lib/design_system/app_breakpoints.dart
  - lib/screen/network_calculator_screen.dart
  - test/app/destination_catalog_test.dart
  - test/app/app_shell_test.dart
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-08
must_haves:
  truths:
    - "O usuário abre Rede, Armazenamento e Hash pelo mesmo catálogo tipado em português (D-01, UI-01, UI-08)."
    - "A navegação usa NavigationBar abaixo de 600 px, rail recolhido de 600 a 839 px e rail extended a partir de 840 px (D-02, D-14, UI-02)."
    - "Uma interação real na Calculadora de Rede continua disponível depois de trocar de destino ou cruzar um breakpoint (D-04)."
    - "Com cinco destinos, a navegação compacta mostra três prioridades e Ferramentas, sem criar listas paralelas nem nova arquitetura de rotas (D-03)."
  artifacts:
    - path: "lib/app/app_destinations.dart"
      provides: "AppDestination, AppDestinationCategory e catálogo concreto único"
      contains: "appDestinations"
    - path: "lib/app/app_shell.dart"
      provides: "Shell adaptativo com preservação de páginas"
      contains: "IndexedStack"
    - path: "lib/design_system/app_breakpoints.dart"
      provides: "Contrato central de largura 600/840"
      contains: "AppWidthClass"
    - path: "test/app/app_shell_test.dart"
      provides: "Evidência end-to-end do catálogo até uma ferramenta offline existente"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app/app_shell.dart"
      via: "MaterialApp.home recebe AppShell"
      pattern: "home:.*AppShell"
    - from: "lib/app/app_shell.dart"
      to: "lib/app/app_destinations.dart"
      via: "AppShell consome List<AppDestination> e deriva chrome/páginas"
      pattern: "List<AppDestination>"
    - from: "lib/app/app_shell.dart"
      to: "lib/design_system/app_breakpoints.dart"
      via: "classificação de constraints"
      pattern: "AppBreakpoints\.classify"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** localizar e abrir cada ferramenta por uma navegação clara e adaptativa, **so that** eu resolva uma tarefa offline sem barra superlotada nem perda do estado em andamento.

<objective>
Entregar o walking skeleton brownfield catálogo tipado → shell adaptativo → interação real em uma ferramenta existente, implementando D-01, D-02, D-03, D-04, D-14 e a correção pt-BR exigida por UI-08.

Purpose: Provar cedo a restrição central da fase: o chrome pode mudar com a largura sem recriar as screens ou alterar sua lógica funcional.
Output: Testes RED/GREEN, catálogo tipado único, breakpoints centrais, shell Material 3 integrado e título da Calculadora de Rede em pt-BR.
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-CONTEXT.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-RESEARCH.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-PATTERNS.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/SKELETON.md
@lib/main.dart
@lib/screen/network_calculator_screen.dart
@lib/screen/data_converter_screen.dart
@lib/screen/hash_generator_screen.dart

<interfaces>
Current working-copy contracts that must be preserved:
- `App` currently owns `MaterialApp`, `lightTheme`, `darkTheme`, the Portuguese product title, and the user's three destinations including Hash.
- `NetworkCalculatorScreen`, `DataConverterScreen`, and `HashGeneratorScreen` are existing `StatefulWidget` destinations whose calculation/controller logic stays untouched.

Contracts created by this plan:
- `enum AppDestinationCategory` includes the typed category used by the future catalog.
- `class AppDestination` has stable `id`, pt-BR `label`, `semanticLabel`, outlined `icon`, filled `selectedIcon`, typed `category`, integer `compactPriority`, and `WidgetBuilder pageBuilder`.
- `appDestinations` is an immutable list of `AppDestination`, ordered Rede → Armazenamento → Hash.
- `enum AppWidthClass { compact, medium, expanded }` and `AppBreakpoints.classify(double)` implement `less than 600`, `600 through 839`, and `840 or greater`.
- `AppShell` requires a list of `AppDestination`, materializes pages once, and owns only selected-index/navigation presentation state.
</interfaces>

<dirty_worktree_guard>
The user already modified `lib/main.dart` to add Hash and Portuguese labels and modified `lib/screen/network_calculator_screen.dart` with calculation, output, keyboard and disposal fixes. Treat the working copy as authoritative. Before editing either file, run `git diff -- {file}`; apply surgical patches only; never restore from HEAD, replace the file wholesale, run checkout/reset/clean, or stage unrelated paths. This plan does not touch `pubspec.yaml`, generated plugin files, services, models, or the other two screens.
</dirty_worktree_guard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing end-to-end navigation contract</name>
  <files>test/app/destination_catalog_test.dart, test/app/app_shell_test.dart</files>
  <read_first>
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — Adaptive Layout Contract, Navigation and Destination Catalog, Widget and Golden Verification
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-PATTERNS.md — app catalog/shell assignments and User-Change Overlap Risks
    - lib/main.dart — current three-destination bootstrap; preserve the user's Hash work
    - lib/screen/network_calculator_screen.dart — real offline interaction used by the happy path
    - test/network_calculator_test.dart — local test naming and assertion style
  </read_first>
  <behavior>
    - Happy path: at 360×800, pump the real `App`, confirm `NavigationBar`, enter a valid IP/CIDR, calculate, navigate to another catalog destination and back, and observe the same result.
    - Catalog: ids are unique/stable, labels and semantic labels are nonempty pt-BR, icons are paired, order is Rede/Armazenamento/Hash, and each factory builds its current screen.
    - Boundaries: 599 uses Bar; 600/839 use collapsed Rail; 840 uses extended Rail.
    - Preservation: a stateful fake destination is not recreated when selecting another destination, tapping the selected item, or resizing.
    - Growth: five fake destinations produce three compact priorities plus Ferramentas; choosing an unpinned item through the categorized catalog keeps Ferramentas selected.
  </behavior>
  <action>Following MVP mode, create these tests before production code. Use `tester.view.physicalSize`, DPR 1, and `addTearDown(tester.view.reset)`; do not use deprecated TestWindow APIs. Exercise the real current `App` for the first happy path and small fake destinations for exact boundary/growth assertions. The test must fail for the expected absence of D-01/D-02/D-03/D-04 contracts, not because of a malformed test or unavailable package. Do not modify existing service tests.</action>
  <acceptance_criteria>
    - The happy path demonstrates catalog → adaptive shell → real existing calculation, not a mocked service.
    - Assertions cover 599/600/839/840, state preservation, repeated selection, all three current destinations, and five-destination overflow.
    - Running the targeted test is RED for the missing new contracts and no unrelated baseline test is changed.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub test/app/destination_catalog_test.dart test/app/app_shell_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before catalog/shell implementation' }</automated>
  </verify>
  <done>The failing tests express the entire user-visible shell contract and fail only because the new catalog/shell artifacts do not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the adaptive offline slice pass without rewriting screens</name>
  <files>lib/main.dart, lib/app/app_destinations.dart, lib/app/app_shell.dart, lib/design_system/app_breakpoints.dart, lib/screen/network_calculator_screen.dart</files>
  <read_first>
    - test/app/destination_catalog_test.dart — exact RED catalog contract from Task 1
    - test/app/app_shell_test.dart — exact RED happy path, breakpoint and growth contract from Task 1
    - lib/main.dart — current dirty baseline containing the user-added Hash destination
    - lib/model/analysis_result.dart — immutable local model analog with const constructor and final fields
    - lib/screen/network_calculator_screen.dart — current dirty baseline; only its visible AppBar title may change
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — catalog fields, growth rule, selection/preservation and layout invariants
  </read_first>
  <action>First capture `git diff -- lib/main.dart lib/screen/network_calculator_screen.dart`. Deliberately consolidate the PATTERNS map's suggested `tools_app.dart` into the existing `lib/main.dart` and its suggested `app_destination.dart` into `lib/app/app_destinations.dart`: keep the exact current bootstrap analog `main()` → `runApp(const App())` and the existing `App`/`MaterialApp` ownership of title, `theme`, `darkTheme` and debug flag, while defining `AppDestination` beside the one concrete catalog as a small immutable value object following `lib/model/analysis_result.dart`'s `const` constructor + `final` fields pattern. This consolidation avoids two extra single-purpose files without weakening the catalog/shell boundaries. Implement D-01 in `app_destinations.dart` with the exact typed fields and a single immutable catalog importing the three current screens. Implement D-14 in `app_breakpoints.dart`. Implement D-02/D-04 in `AppShell`: materialize page builders once in `initState`, retain one `IndexedStack`, derive Bar/Rail destinations from the same catalog, guard repeated selection, and change only navigation chrome on resize. Implement D-03 completely: at five or more destinations show the three highest compact priorities plus Ferramentas; open a categorized Material modal/list derived from that same catalog; selecting an unpinned destination updates the stack while Ferramentas remains selected. Surgically change `App` so its existing `MaterialApp` uses `AppShell(destinations: appDestinations)` while preserving title/themes/debug flag and all user-added destinations. In the network screen change only `Network Calculator` to `Calculadora de Rede` per UI-08; do not touch validation, controllers, calculation, layout, results, imports, or disposal. Add no router, package, backend, persistence, network call, analytics, permission, or future Diagnóstico destination.</action>
  <acceptance_criteria>
    - The real compact happy path calculates a network, navigates away/back and preserves result state.
    - Bar/Rail transitions and the complete five-destination Ferramentas behavior match the UI-SPEC.
    - `main.dart` no longer contains parallel screen/item lists but still contains the user's Portuguese product setup.
    - The network screen diff contains only the one title-line semantic change relative to its Task 2 starting point.
    - All pre-existing tests remain green and no pubspec/generated/source file outside the declared list changes.
  </acceptance_criteria>
  <verify>
    <automated>dart format lib/main.dart lib/app/app_destinations.dart lib/app/app_shell.dart lib/design_system/app_breakpoints.dart lib/screen/network_calculator_screen.dart test/app/destination_catalog_test.dart test/app/app_shell_test.dart
flutter analyze --no-pub
flutter test --no-pub test/app/destination_catalog_test.dart test/app/app_shell_test.dart
flutter test --no-pub</automated>
  </verify>
  <done>UI-01/UI-02/UI-08 and D-01/D-02/D-03/D-04/D-14 are proven by green tests through a real existing offline interaction, with the dirty working copy preserved.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `lib/app/app_destinations.dart` | `AppDestinationCategory`, `AppDestination`, `appDestinations` |
| `lib/app/app_shell.dart` | `AppShell` and its adaptive catalog presentation |
| `lib/design_system/app_breakpoints.dart` | `AppWidthClass`, `AppBreakpoints` |
| `test/app/destination_catalog_test.dart` | Catalog contract tests |
| `test/app/app_shell_test.dart` | Brownfield end-to-end, breakpoint, preservation and growth tests |
| `lib/main.dart` | Existing `App` rewired to the shell; no parallel catalog |
| `lib/screen/network_calculator_screen.dart` | Surgical pt-BR title correction only |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Typed catalog → navigation chrome | Labels/icons/factories must identify the same destination in Bar, Rail and Ferramentas. |
| Navigation event → selected page | User-selected indices cross into preserved page state and must stay in range. |
| Dirty working copy → phase patch | Pre-existing uncommitted functionality must not be overwritten by planned extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `appDestinations` / `AppShell` | mitigate | Derive label, icon, semantic label and page from one typed object; assert ids/order/factories and selection in tests. |
| T-01-02 | Tampering | compact overflow selection | mitigate | Map visible entries back to stable catalog indices, guard bounds/repeated selection, and test an unpinned fifth destination. |
| T-01-03 | Denial of Service | page lifecycle/state | mitigate | Materialize offline pages once in `initState`; resize only chrome; document that future I/O visibility/cancellation belongs to Phase 3. |
| T-01-04 | Tampering | dirty source overlap | mitigate | Require pre-edit diffs, surgical patches and path-scoped staging; prohibit reset/checkout/whole-file restoration. |
| T-01-SC | Tampering | package supply chain | accept | This plan performs no package-manager install and must not edit `pubspec.yaml` or lockfiles. |
</threat_model>

<verification>
1. RED is observed before implementation for the new shell contract.
2. GREEN targeted tests prove the real compact calculation path, breakpoints, state preservation and growth behavior.
3. `flutter analyze --no-pub` and the full `flutter test --no-pub` suite pass.
4. `git diff -- lib/main.dart lib/screen/network_calculator_screen.dart` confirms preservation of the user's pre-existing work and the one-line screen-title boundary.
</verification>

<success_criteria>
- A user can open and operate any current destination through one adaptive Material 3 shell.
- Existing network calculation state survives navigation and resizing.
- All navigation copy visible in this slice is pt-BR, including `Calculadora de Rede`.
- The five-destination growth behavior is implemented now rather than deferred or hard-coded separately.
- No source outside the declared list, package, external service or Phase 2/3 functionality is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-01-SUMMARY.md` when done.
</output>
