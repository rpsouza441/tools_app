---
phase: 01-contrato-visual-e-funda-o-adaptativa
plan: "11"
type: execute
wave: 4
depends_on:
  - 01-06
  - 01-10
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
must_haves:
  truths:
    - "Um humano reexecuta os sete checks do 01-05-PLAN depois de 01-06..01-10: 360/599, 600/720/839, 840/1024, claro/escuro, texto 200%, preservação offline, TalkBack, anúncios/targets, goldens ao vivo."
    - "TalkBack anuncia navegação com semanticLabel, sete estados canônicos, Copiar {rótulo} e exatamente um {Rótulo} copiado, sem nós duplicados."
    - "Se não houver dispositivo/emulador Android, o gate permanece pendente; não há autoaprovação nem --auto."
  artifacts:
    - path: "test/manual/design_system_app.dart"
      provides: "Harness device-only já atualizado no 01-10 (copy + pt-BR)"
    - path: ".planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-11-SUMMARY.md"
      provides: "Só existe após approved humano explícito"
  key_links:
    - from: "test/manual/design_system_app.dart"
      to: "test/design_system/design_system_gallery.dart"
      via: "harness monta a galeria pública com copy"
      pattern: "DesignSystemGallery"
    - from: "lib/main.dart"
      to: "lib/app/app_shell.dart"
      via: "app real para preservação offline e chrome"
      pattern: "AppShell"
---

## Phase Goal

**As a** profissional de TI, **I want to** usar a fundação adaptativa e acessível, **so that** eu opere as ferramentas com clareza.

<objective>
Fechar o gap H: gate humano Android/TalkBack que o 01-05 autoaprovou sem dispositivo.

Purpose: Widget tests e goldens não provam densidade, TalkBack real nem correspondência visual percebida (per D-16, D-17, D-18).
Output: `approved` explícito do usuário após os sete checks, ou gate pendente se não houver Android. Sem `--auto`. Sem SUMMARY de sucesso se qualquer check falhar ou for pulado.
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-05-PLAN.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-05-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-06-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-07-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-08-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-09-SUMMARY.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-10-SUMMARY.md
@test/manual/design_system_app.dart
@test/design_system/goldens

<interfaces>
O 01-05-SUMMARY registrou autoaprovação por `--auto` sem os sete checks. Este plano os reexecuta DEPOIS de 01-06..01-10.

Caminho golden para o check 7: `test/design_system/goldens/` (override do 01-04/01-10). Não usar `test/goldens/`.

Escopo Phase 1: fundação (shell, tema, primitives, galeria). Não exigir que as três telas de produção já usem ToolScaffold (Phase 2 / nota UI-03 do 01-06).

Se `flutter devices` não listar um device/emulador Android, parar: checkpoint permanece blocking/pending. Não inventar aprovação. Não passar `--auto` ao execute-plan.
</interfaces>

<dirty_worktree_guard>
Plano somente de verificação. Não editar, não regenerar goldens, não formatar, não commit, não checkout/reset/clean, não tocar `pubspec.lock-old`. Se um check falhar, registrar viewport/tema/escala/passos e deixar o plano incompleto.
</dirty_worktree_guard>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Reexecutar os sete checks em Android/TalkBack após os gaps 01-06..01-10</name>
  <files>none</files>
  <read_first>
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md — Manual gate, Visual Acceptance Criteria
    - .planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-10-SUMMARY.md — goldens canônicos e harness
    - test/manual/design_system_app.dart — comando do harness
  </read_first>
  <action>Rodar o preflight automatizado abaixo e parar se falhar. Listar `flutter devices`. Se não houver Android, registrar pendência e NÃO aprovar. Caso haja device, executar o app real e o harness com TalkBack. Não usar --auto. Não alterar arquivos. Distinguir issue de fundação (Phase 1) de anatomia local das três telas (Phase 2). Só a resposta explícita `approved` do usuário após todos os checks conclui a task.</action>
  <what-built>Catálogo imutável e shell com overflow só em compacta, semanticLabel e labels curtas, temas e sete estados canônicos, cópia com lifecycle seguro e copyWriter real, ToolMetric refluível, galeria pt-BR com copy, goldens em test/design_system/goldens/, Goal MVP travado em docs.</what-built>
  <how-to-verify>
    1. Larguras: `flutter run` em superfície redimensionável. Em 360 e 599 confirmar NavigationBar (com 3 destinos reais; labels Rede / Armazenamento / Hash). Em 600, 720 e 839 confirmar rail recolhido com todos os destinos visíveis/roláveis, sem Ferramentas. Em 840 e 1024 confirmar rail extended. Nada coberto pela barra/rail.
    2. Preservação offline: na Calculadora de Rede, informar IP/CIDR válidos, calcular, ir a Armazenamento e Hash e voltar, cruzar 600 e 840; inputs e resultado permanecem.
    3. Temas: alternar claro/escuro. Texto de corpo neutro (não neon); verde só no acento reservado; cards sem sombra; foco/erro/disabled/selected legíveis; canvases percebidos como painel calmo (`#F7F9F7` / `#0F1511`).
    4. Texto 200%: repetir navegação e galeria; sem overflow horizontal nem ellipsis essencial; tudo alcançável por scroll vertical, inclusive métricas longas e Indisponível.
    5. TalkBack: `flutter devices`, depois `flutter run -t test/manual/design_system_app.dart -d {android-device-id}`. Percorrer navegação → título/resumo → campos → ações (`Calcular rede`) → sete estados (headings canônicos: Nenhum resultado ainda, Processando, Concluído, Não foi possível concluir, Sem conexão com a internet, Permissão necessária, Operação cancelada) → resultado/métrica → valor técnico → copiar.
    6. Anúncios e targets: destino selecionado com nome completo (semanticLabel) uma vez, sem duplicar tooltip+semantics; `Copiar {rótulo}`; exatamente um `{Rótulo} copiado`; SnackBar alheio não é o foco deste check mas a confirmação de cópia deve ser a única de cópia; todos os icon buttons confortáveis (~48×48).
    7. Comparação visual ao vivo, com pares explícitos — não exigir que `flutter run` (catálogo de produção, 3 destinos) coincida com o PNG de shell de 5 destinos. Pares: (a) `primitives_compact_light.png` e `primitives_compact_dark.png` contra a galeria/harness (`flutter run -t test/manual/design_system_app.dart`); (b) `shell_medium_light.png` e `shell_expanded_dark.png` contra a fixture de 5 destinos do widget test em `design_system_golden_test.dart`, não contra o app real. O app real em 600/840 deve listar os 3 destinos de produção no rail, sem Ferramentas; o overflow compacto de 5 destinos permanece coberto pelos testes do 01-07 (CR-01), não por este PNG. Rejeitar clipping, overflow, bordas agressivas, campos esticados, ou galeria/harness sem botão de copiar.
  </how-to-verify>
  <acceptance_criteria>
    - Preflight analyzer + suíte + goldens verde.
    - Os sete checks foram executados em dispositivo/emulador Android com TalkBack, ou o gate ficou pending por ausência de Android (não approved).
    - Nenhuma autoaprovação, nenhum `--auto`.
    - Issue reportada inclui device, viewport, tema, text scale, passos, observado vs esperado.
    - Conclusão exige `approved` explícito do usuário.
  </acceptance_criteria>
  <verify>
    <automated>dart format --output=none --set-exit-if-changed lib/main.dart lib/app lib/design_system lib/theme/theme.dart lib/screen/network_calculator_screen.dart test/app test/design_system test/manual test/theme
flutter analyze --no-pub
flutter test --no-pub test/design_system/design_system_golden_test.dart
flutter test --no-pub</automated>
    <human-check>Completar os sete checks numerados no app real e no harness Android com TalkBack. Se flutter devices não mostrar Android, responder que o gate permanece pending — não approved.</human-check>
  </verify>
  <resume-signal>Type "approved" if every check passes on Android/TalkBack. If there is no Android device, say "pending — no Android device" (not approved). If a check fails, give step, device, viewport, theme, text scale, observed and expected.</resume-signal>
  <done>O usuário responde `approved` após todos os checks, ou o checkpoint permanece pending/bloqueado; autoaprovação não conta.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role | Owner |
|---|---|---|
| `01-11-SUMMARY.md` | Registro de aprovação humana (somente após approved) | 01-11 |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Fundação acessível operável | 01-11 | COVERED | Gate humano. |
| REQ | UI-01..UI-09 | Sign-off perceptivo | 01-11 | COVERED | Não marca REQUIREMENTS complete. |
| CONTEXT | D-16 D-17 D-18 | TalkBack, escala, goldens revisados | 01-11 | COVERED | |
| VERIFIER | Gap H | 01-05 autoaprovado | 01-11 | COVERED | Sem --auto. |

Deferred: adoção das primitives nas três telas (Phase 2).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testes verdes → aprovação humana | Preflight não substitui TalkBack nem inspeção visual. |
| Harness → produto | design_system_app.dart não é destino de produção. |
| Ausência de Android → status do plano | Pending ≠ approved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G11-01 | Spoofing | aprovação humana | mitigate | Resume-signal exige approved; pending se não houver Android; proibir --auto. |
| T-01-G11-02 | Repudiation | check falho | mitigate | Exigir device/viewport/tema/escala/passos/observado/esperado. |
| T-01-G11-03 | Tampering | worktree no checkpoint | mitigate | Somente leitura; proibir edits/goldens/commits. |
| T-01-G11-04 | Information disclosure | TalkBack/clipboard | accept | Sem telemetria; clipboard só do valor da fixture/produção local. |
| T-01-SC | Tampering | packages | accept | Sem install. |
</threat_model>

<verification>
1. Preflight verde sem editar arquivos.
2. Humano completa os sete checks ou declara pending por falta de Android.
3. SUMMARY de sucesso só após `approved`.
</verification>

<success_criteria>
- Gap H fechado somente com evidência humana real, ou explicitamente pending.
- Phase 1 ainda não é marcada Complete por este plano sozinho se o usuário não aprovou.
- Nenhuma tela de produção foi migrada durante o checkpoint.
</success_criteria>

<output>
Create `.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-11-SUMMARY.md` only after the user explicitly responds `approved`. If Android is missing, do not create a successful SUMMARY; leave the checkpoint pending. If a check fails, do not create a successful SUMMARY; route the reproducible issue.
</output>
