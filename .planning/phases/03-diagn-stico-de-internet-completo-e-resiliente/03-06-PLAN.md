---
phase: 03-diagn-stico-de-internet-completo-e-resiliente
plan: "06"
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - lib/diagnostic/diagnostic_defaults.dart
  - lib/diagnostic/platform/android_network_snapshot_source.dart
  - lib/diagnostic/session/diagnostic_session_impl.dart
  - lib/screen/internet_diagnostic_screen.dart
  - test/screen/internet_diagnostic_screen_test.dart
  - test/diagnostic/android_network_snapshot_source_test.dart
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03
  - DIAG-04
  - DIAG-05
  - DIAG-08
  - DIAG-09
  - DIAG-10
  - DIAG-11
  - QUAL-01
  - QUAL-05
  - QUAL-08
must_haves:
  truths:
    - "Produção no Android usa snapshot Kotlin + DioPublicIpSource + TcpConnectGatewayProbe + DioHttpsInternetProbe; não-Android permanece unavailable honesto (D-04, D-05, QUAL-05)."
    - "A UI mostra transporte, INTERNET, validated e captive separados, IPv4 local, gateway, IPv4 público com provedor ipify e horário, probes com TCP connect/HTTPS, min/média/máx e denominador, ICMP Indisponível, sem ping (DIAG-02..09, D-10, D-12, D-13)."
    - "Offline de plataforma (sem rede ativa) mapeia ToolStatusVariant.offline e não gira spinner infinito (QUAL-01)."
    - "Falha parcial do ipify com probe HTTPS ok mostra os dois fatos; copy não diz internet completa (DIAG-10, D-13)."
  artifacts:
    - path: "lib/diagnostic/diagnostic_defaults.dart"
      provides: "createSession de produção com adaptadores reais"
      contains: "DioPublicIpSource"
    - path: "lib/screen/internet_diagnostic_screen.dart"
      provides: "Copy de honestidade, provenance e ICMP"
      contains: "api.ipify.org"
  key_links:
    - from: "lib/diagnostic/diagnostic_defaults.dart"
      to: "lib/diagnostic/platform/android_network_snapshot_source.dart"
      via: "Platform.isAndroid ? Android wrap : Unsupported"
      pattern: "AndroidNetworkSnapshotSource"
    - from: "lib/screen/internet_diagnostic_screen.dart"
      to: "lib/design_system/tool_status_panel.dart"
      via: "offline/failure/success + preservedChild"
      pattern: "ToolStatusVariant.offline"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** executar um diagnóstico de conectividade honesto, progressivo, cancelável e resistente a falhas parciais, **so that** eu veja evidências independentes sem conclusões enganosas.

<objective>
Ligar os adaptadores reais na composição de produção e completar a honestidade visual: fatos Android, ipify, TCP/HTTPS, agregação, ICMP indisponível, offline e falha parcial (D-13, DIAG-02..11, QUAL-01, QUAL-05). Sem lifecycle de IndexedStack ainda (03-07).

Purpose: Depois desta fatia o default do app não é mais stub: um run de produção mede snapshot + IP + probes. A UI deixa de parecer um placeholder.
Output: DiagnosticDefaults de produção, wrap AndroidSnapshotFacts → NetworkSnapshotSource, copy pt-BR, testes de widget de provenance/offline/parcial.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-02-SUMMARY.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-03-SUMMARY.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-04-SUMMARY.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-05-SUMMARY.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md
@lib/diagnostic/diagnostic_defaults.dart
@lib/screen/internet_diagnostic_screen.dart
@lib/diagnostic/platform/android_network_snapshot_source.dart
@lib/diagnostic/platform/unsupported_network_snapshot_source.dart

<interfaces>
DiagnosticDefaults.createSession() produção:
- snapshot: se dart:io Platform.isAndroid, adapter que implements NetworkSnapshotSource usando AndroidNetworkSnapshotSource + parser 03-02 mapeando AndroidSnapshotFacts → NetworkSnapshot (transports, hasInternet, validated, captivePortal, notMetered, localIpv4, gatewayIpv4, networkHandle, hasActiveNetwork). Caso contrário UnsupportedNetworkSnapshotSource.
- localIpv4: SnapshotLocalIpv4Source
- gateway: SnapshotDefaultGatewaySource
- publicIp: DioPublicIpSource(Dio(BaseOptions from PublicIpConfig.defaults), PublicIpConfig.defaults)
- gatewayProbe: TcpConnectGatewayProbe(GatewayProbeConfig.defaults)
- internetProbe: DioHttpsInternetProbe(Dio separado ou o mesmo sem keep-alive, HttpsProbeConfig.defaults)
- aggregator: LatencyAggregator()
- kIsWeb: tratar como não-Android → unsupported snapshot; não prometer desktop (D-04). Prefer dart.library.io && Platform.isAndroid.

AndroidNetworkSnapshotSource deve passar a implements NetworkSnapshotSource neste plano se 03-02 deixou só currentFacts(). Mapear watching: startWatching/stopWatching do canal.

UI copy (03-RESEARCH Honest definition, pt-BR):
- Nunca badge Online único.
- IPv4 público label IPv4 público; metadata inclui ipify (api.ipify.org) e horário.
- Probe gateway: TCP connect, alvo = IP, porta, timeout 2s, texto de limitação L3.
- Probe internet: HTTPS, URL/host gstatic, timeout 5s, conexão fria.
- ICMP: Indisponível neste MVP — ToolMetric value null.
- Teste de widget: find.text('ping') findsNothing na tela com fatos TCP/HTTPS preenchidos (usar fake session com provenance TCP connect / HTTPS).
- Agregados: ToolMetric min/média/máx ms e texto tipo 3/4 sucessos (DIAG-09).
- Snapshot hasActiveNetwork false → phase offline na sessão (ajustar DiagnosticSessionImpl se ainda não mapeia) e ToolStatusVariant.offline; fatos unavailable, QUAL-01 sem spinner eterno (timeouts já nos adapters).
- permissionDenied no modelo se um fato vier com esse status; não pedir ACCESS_LOCAL_NETWORK.

Não adicionar speed test destination. Não analytics. Não SSID. Não pubspec.lock-old.
Flutter CLI: C:\src\flutter\bin\flutter.bat
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composição de produção e wrap do snapshot Android</name>
  <files>lib/diagnostic/diagnostic_defaults.dart, lib/diagnostic/platform/android_network_snapshot_source.dart, test/diagnostic/android_network_snapshot_source_test.dart</files>
  <read_first>
    - lib/diagnostic/diagnostic_defaults.dart
    - lib/diagnostic/platform/android_network_snapshot_source.dart
    - lib/diagnostic/platform/unsupported_network_snapshot_source.dart
    - lib/diagnostic/contracts/network_snapshot_source.dart
    - lib/diagnostic/public_ip/dio_public_ip_source.dart
    - lib/diagnostic/probes/tcp_connect_probe.dart
    - lib/diagnostic/probes/dio_https_probe.dart
  </read_first>
  <action>Fazer AndroidNetworkSnapshotSource implements NetworkSnapshotSource: current() converte AndroidSnapshotFacts em NetworkSnapshot; watching encaminha o callback. Em DiagnosticDefaults.createSession, injetar os quatro adaptadores reais e os leitores 2–3 de snapshot (D-05). Fora de Android, Unsupported + ainda assim DioPublicIpSource/probes HTTPS podem existir em desktop dart:io, mas o compromisso D-04 é unavailable para snapshot local/gateway; planner: probes HTTPS e ipify só se Platform.isAndroid no MVP para não prometer o não verificado (03-RESEARCH Open Question 4). Portanto: em não-Android, manter UnavailablePublicIpSource + Unavailable*Probe + Unsupported snapshot. Em Android, todos reais. Atualizar testes do parser para o wrap NetworkSnapshot. Não criar NetworkService. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - DiagnosticDefaults em Android constrói os adapters reais; fora, stubs/unsupported.
    - AndroidNetworkSnapshotSource satisfaz NetworkSnapshotSource.
    - Sete contratos continuam tipos distintos.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/android_network_snapshot_source_test.dart test/diagnostic/diagnostic_session_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub lib/diagnostic</automated>
  </verify>
  <done>Produção Android mede snapshot+IP+probes via contratos separados (D-04, D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honestidade da UI — provenance, ICMP, offline, parcial</name>
  <files>lib/screen/internet_diagnostic_screen.dart, lib/diagnostic/session/diagnostic_session_impl.dart, test/screen/internet_diagnostic_screen_test.dart</files>
  <read_first>
    - lib/screen/internet_diagnostic_screen.dart
    - lib/design_system/tool_status_panel.dart
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Honest definition copy
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Fake session com TCP connect + HTTPS + icmp unavailable: tela mostra TCP connect, HTTPS, Indisponível para ICMP, findsNothing para ping.
    - Fake public IP failure + HTTPS success: ambos visíveis; não há texto que afirme internet completa.
    - Fake snapshot hasActiveNetwork false: ToolStatusVariant.offline (heading Sem conexão com a internet), primary Repetir habilitado, sem CircularProgressIndicator infinito após o frame terminal.
    - Fake agregados 3/4 sucessos: texto do denominador visível.
    - IPv4 público mostra ipify (api.ipify.org).
    - Gateway probe failure ainda mostra o endereço do gateway se o fato gateway for success (Pitfall 6, DIAG-10).
  </behavior>
  <action>Completar a tela com metadata de ProbeProvenance, ToolMetric de min/média/máx e denominador, copy de ipify, limitação TCP, ICMP Indisponível neste MVP (DIAG-08, DIAG-09, D-10, D-12, D-13). Mapear sessão sem rede ativa para offline (ajustar DiagnosticSessionImpl.phase se necessário). PreservedChild continua só loading/failure/cancelled; success usa card irmão (DIAG-11). Não usar onRetry. Não pedir permissão LAN. Estender internet_diagnostic_screen_test com os casos QUAL-08 offline, parcial, invalid-display, missing gateway (endereço ausente + probe não rotulado como morto). Garantir que testes PRES ainda passam se a tela default agora cria Dio — widget tests devem injetar FakeDiagnosticSession para não sair à rede. InternetDiagnosticScreen de produção no catálogo pode criar sessão real; testes de catálogo pageBuilder só checam o tipo, não disparam start. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - Widget tests novos passam e findsNothing ping.
    - Offline e parcial não deixam spinner eterno.
    - Provedor ipify visível junto do IPv4 público.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/internet_diagnostic_screen_test.dart test/screen/tools_preservation_test.dart test/diagnostic/
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>UI honesta: fatos independentes, métodos reais, ICMP unavailable, offline e parciais visíveis (DIAG-02..11, QUAL-01, QUAL-05, QUAL-08).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | Role |
|---|---|
| `diagnostic_defaults.dart` | Composição Android real vs stubs |
| `android_network_snapshot_source.dart` | implements `NetworkSnapshotSource` |
| `internet_diagnostic_screen.dart` | Copy DIAG-08/09/13 e ICMP |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tela de produção → internet | Iniciar dispara I/O real no Android. |
| Composição → terceiros | ipify e gstatic recebem GET. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Information Disclosure | UI copy | mitigate | Divulgar api.ipify.org; não fingir que IP público prova toda a internet (D-12, D-13). |
| T-03-22 | Tampering | badge Online | mitigate | Três famílias de fatos; sem isOnline (DIAG-02, D-13). |
| T-03-23 | Denial of Service | spinner | mitigate | Timeouts nos adapters + phase terminal offline/failure (QUAL-01). |
| T-03-SC | Tampering | supply chain | accept | Sem pacote novo. |
</threat_model>

<verification>
1. Defaults Android referenciam os quatro adapters reais e os dois leitores de snapshot.
2. Widget tests: ping ausente, ipify visível, offline terminal, parcial visível.
3. analyze + diagnostic tests verdes.
</verification>

<success_criteria>
- O diagnóstico de produção no Android é honesto e completo em fatos, ainda sem share/lifecycle de shell.
- QUAL-05: não-Android não inventa 0 ms nem gateway.
</success_criteria>

<output>
Create `.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-06-SUMMARY.md` when done
</output>
