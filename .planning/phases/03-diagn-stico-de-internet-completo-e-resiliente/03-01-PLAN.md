---
phase: 03-diagn-stico-de-internet-completo-e-resiliente
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/diagnostic/models/diagnostic_fact.dart
  - lib/diagnostic/models/diagnostic_run_state.dart
  - lib/diagnostic/models/latency_aggregate.dart
  - lib/diagnostic/models/probe_provenance.dart
  - lib/diagnostic/models/network_snapshot.dart
  - lib/diagnostic/contracts/network_snapshot_source.dart
  - lib/diagnostic/contracts/local_ipv4_source.dart
  - lib/diagnostic/contracts/default_gateway_source.dart
  - lib/diagnostic/contracts/public_ip_source.dart
  - lib/diagnostic/contracts/gateway_probe.dart
  - lib/diagnostic/contracts/internet_probe.dart
  - lib/diagnostic/contracts/diagnostic_session.dart
  - lib/diagnostic/contracts/share_text_port.dart
  - lib/diagnostic/aggregation/latency_aggregator.dart
  - lib/diagnostic/session/cancellation_scope.dart
  - lib/diagnostic/session/diagnostic_session_impl.dart
  - lib/diagnostic/platform/unsupported_network_snapshot_source.dart
  - lib/diagnostic/local/snapshot_local_ipv4_source.dart
  - lib/diagnostic/gateway/snapshot_default_gateway_source.dart
  - lib/diagnostic/public_ip/unavailable_public_ip_source.dart
  - lib/diagnostic/probes/unavailable_gateway_probe.dart
  - lib/diagnostic/probes/unavailable_internet_probe.dart
  - lib/diagnostic/share/noop_share_text_port.dart
  - lib/diagnostic/diagnostic_defaults.dart
  - test/diagnostic/fakes.dart
  - test/diagnostic/latency_aggregator_test.dart
  - test/diagnostic/diagnostic_session_test.dart
autonomous: true
requirements:
  - DIAG-01
  - DIAG-09
  - DIAG-10
  - DIAG-12
  - DIAG-14
  - QUAL-02
  - QUAL-05
  - QUAL-08
must_haves:
  truths:
    - "Uma sessão inicia só um run por vez; o segundo start enquanto running é no-op (DIAG-01, D-08)."
    - "Fatos irmãos sobrevivem quando um contrato falha; ICMP e plataformas sem snapshot ficam indisponíveis, nunca 0 ms ou IP inventado (DIAG-10, QUAL-05, D-04, D-05, D-10)."
    - "cancel() aborta I/O registrado no CancellationScope e ignora conclusão tardia com runId antigo (DIAG-12, QUAL-02, D-07)."
    - "Agregação expõe min/média/máx só de sucessos e sucessos/falhas com denominador explícito (DIAG-09)."
    - "startedAt/finishedAt existem só em memória; nenhum SharedPreferences/arquivo/DB (DIAG-14, D-09, D-11)."
  artifacts:
    - path: "lib/diagnostic/contracts/network_snapshot_source.dart"
      provides: "Contrato 1 NetworkSnapshotSource"
      exports: ["NetworkSnapshotSource"]
    - path: "lib/diagnostic/contracts/local_ipv4_source.dart"
      provides: "Contrato 2 LocalIpv4Source"
      exports: ["LocalIpv4Source"]
    - path: "lib/diagnostic/contracts/default_gateway_source.dart"
      provides: "Contrato 3 DefaultGatewaySource"
      exports: ["DefaultGatewaySource"]
    - path: "lib/diagnostic/contracts/public_ip_source.dart"
      provides: "Contrato 4 PublicIpSource"
      exports: ["PublicIpSource"]
    - path: "lib/diagnostic/contracts/gateway_probe.dart"
      provides: "Contrato 5a GatewayProbe"
      exports: ["GatewayProbe"]
    - path: "lib/diagnostic/contracts/internet_probe.dart"
      provides: "Contrato 5b InternetProbe"
      exports: ["InternetProbe"]
    - path: "lib/diagnostic/contracts/diagnostic_session.dart"
      provides: "Contrato 6 DiagnosticSession"
      exports: ["DiagnosticSession"]
    - path: "lib/diagnostic/aggregation/latency_aggregator.dart"
      provides: "Contrato 7 LatencyAggregator"
      exports: ["LatencyAggregator"]
    - path: "lib/diagnostic/session/diagnostic_session_impl.dart"
      provides: "Orquestração com runId e fan-out independente"
      contains: "DiagnosticSessionImpl"
    - path: "test/diagnostic/diagnostic_session_test.dart"
      provides: "QUAL-08 domínio: sucesso, parcial, cancel, tardio, concorrente, gateway ausente"
  key_links:
    - from: "lib/diagnostic/session/diagnostic_session_impl.dart"
      to: "lib/diagnostic/contracts/network_snapshot_source.dart"
      via: "injeção de construtor, sem NetworkService"
      pattern: "NetworkSnapshotSource"
    - from: "lib/diagnostic/session/diagnostic_session_impl.dart"
      to: "lib/diagnostic/session/cancellation_scope.dart"
      via: "register de abort físico por run"
      pattern: "CancellationScope"
    - from: "lib/diagnostic/aggregation/latency_aggregator.dart"
      to: "lib/diagnostic/models/latency_aggregate.dart"
      via: "aggregate de amostras success"
      pattern: "LatencyAggregate"
    - from: "lib/diagnostic/local/snapshot_local_ipv4_source.dart"
      to: "lib/diagnostic/models/network_snapshot.dart"
      via: "lê snapshot; nunca NetworkInterface.list"
      pattern: "fromSnapshot"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** executar um diagnóstico de conectividade honesto, progressivo, cancelável e resistente a falhas parciais, **so that** eu veja evidências independentes sem conclusões enganosas.

<objective>
Criar o domínio injetável dos sete contratos (D-05), o agregador puro (DIAG-09) e a sessão com um run, cancelamento físico e runId (D-06, D-07, D-08), usando stubs que devolvem indisponível — sem monolito NetworkService e sem I/O real ainda.

Purpose: A fatia 03-03 só consegue expor Iniciar/Cancelar ao usuário se a sessão já for testável com fakes. Este plano é a fundação vertical da sessão, não uma camada NetworkService.
Output: modelos, sete contratos, stubs, DiagnosticSessionImpl, fakes manuscritos e testes QUAL-08 de domínio.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-CONTEXT.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-PATTERNS.md
@lib/design_system/copy_value_action.dart
@lib/service/hash_calculator.dart
@test/design_system/copy_lifecycle_test.dart
@test/network_calculator_test.dart

<interfaces>
From lib/design_system/copy_value_action.dart — analog de porta injetável (copiar forma, não o clipboard):
- abstract class CopyValueWriter { Future&lt;void&gt; write(String value); }
- class ClipboardCopyWriter implements CopyValueWriter { const ClipboardCopyWriter(); }

From lib/service/hash_calculator.dart — analog de modelo imutável:
- class HashResult { final fields; const HashResult({...}); }

Criar (este plano) — sete contratos D-05, arquivos separados, package:tools_app/..., sem Flutter nos modelos/agregador/sessão:
- enum DiagnosticFactStatus { success, unavailable, failure, cancelled, timeout, networkChanged, permissionDenied }
- class DiagnosticFact { status, value (String?), message (String?), provenance (ProbeProvenance?), occurredAt (DateTime?) } — const + copyWith opcional
- class ProbeProvenance { method, target, portOrUrl, timeout (Duration), limitations, thirdParty (String?) } — method é literal TCP connect ou HTTPS, nunca ping
- class NetworkSnapshot { hasActiveNetwork (bool), transports (List&lt;String&gt;), hasInternet (bool?), validated (bool?), captivePortal (bool?), notMetered (bool?), localIpv4 (String?), gatewayIpv4 (String?), networkHandle (int?) }
- enum DiagnosticRunPhase { idle, running, success, partialFailure, cancelled, offline }
- class DiagnosticRunState imutável com copyWith: runId (int), phase, startedAt, finishedAt, snapshot, facts nomeados transport / internetCapability / validated / captivePortal / localIpv4 / gateway / publicIpv4 / gatewayProbe / internetProbe / icmp, gatewayLatency, internetLatency. icmp nasce sempre unavailable com mensagem Indisponível neste MVP (D-10).
- class LatencyAggregate { min/avg/max Duration?, plannedAttempts, completedAttempts, successes, failures, timeouts, cancelledCount, networkChangedCount, denominatorLabel (String) }
- abstract class NetworkSnapshotSource { Future&lt;NetworkSnapshot&gt; current(); Future&lt;void&gt; startWatching(void Function(NetworkSnapshot snapshot) onChanged); Future&lt;void&gt; stopWatching(); }
- abstract class LocalIpv4Source { DiagnosticFact fromSnapshot(NetworkSnapshot snapshot); }
- abstract class DefaultGatewaySource { DiagnosticFact fromSnapshot(NetworkSnapshot snapshot); }
- abstract class PublicIpSource { Future&lt;DiagnosticFact&gt; fetch({required int runId, required CancellationScope scope}); }
- abstract class GatewayProbe { Future&lt;ProbeOutcome&gt; probe({required String gatewayIpv4, required int runId, required CancellationScope scope}); }
- abstract class InternetProbe { Future&lt;ProbeOutcome&gt; probe({required int runId, required CancellationScope scope}); }
- class ProbeOutcome { List&lt;DiagnosticFact&gt; samples; LatencyAggregate aggregate; DiagnosticFact summary; }
- abstract class DiagnosticSession { ValueListenable&lt;DiagnosticRunState&gt; get listenable; DiagnosticRunState get state; Future&lt;void&gt; start(); Future&lt;void&gt; cancel(); Future&lt;void&gt; refreshSnapshotOnly(); void dispose(); }
- abstract class ShareTextPort { Future&lt;void&gt; share(String text); }
- class CancellationScope { void register(void Function() abort); Future&lt;void&gt; cancelAll(); bool get isCancelled; }
- class LatencyAggregator { const LatencyAggregator(); LatencyAggregate aggregate(List&lt;DiagnosticFact&gt; samples, {required int plannedAttempts}); }
- DiagnosticSessionImpl construtor exige os sete papéis (snapshot, localIpv4, gateway, publicIp, gatewayProbe, internetProbe, aggregator). Proibido class NetworkService.
- Flutter CLI: C:\src\flutter\bin\flutter.bat
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes falhando do agregador e da sessão</name>
  <files>test/diagnostic/fakes.dart, test/diagnostic/latency_aggregator_test.dart, test/diagnostic/diagnostic_session_test.dart</files>
  <read_first>
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Pattern 2 e 3, QUAL-08, Pitfall 1
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-PATTERNS.md — fakes manuscritos, CompleterCopyWriter analog
    - test/network_calculator_test.dart — group/test/expect
    - test/design_system/copy_lifecycle_test.dart — Completer + dispose
    - test/screen/screen_test_harness.dart — FakeCopyWriter analog
  </read_first>
  <behavior>
    - Agregador: 4 amostras 10/20/30 ms success + 1 timeout → min 10, max 30, avg 20, successes 3, failures 1, plannedAttempts 4, denominator menciona 3 e 4.
    - Agregador: zero sucessos → min/avg/max null, não 0, não throw (QUAL-05).
    - Agregador: amostra cancelled e networkChanged não entram no denominador de falha; contagens próprias visíveis.
    - Sessão start com todos os fakes em success → phase success, publicIpv4/local/gateway/probes success independentes, icmp unavailable, startedAt/finishedAt não-null.
    - Segundo start() enquanto running é no-op: runId não muda (DIAG-01, D-08).
    - Um fake PublicIpSource falha e os outros sucederam → phase partialFailure; fatos de sucesso permanecem (DIAG-10).
    - Snapshot sem gatewayIpv4 → DefaultGatewaySource unavailable, GatewayProbe.probe nunca chamado (DIAG-06).
    - cancel() no meio de Completer: phase cancelled; complete o Completer depois e o estado permanece cancelled (DIAG-12, QUAL-02).
    - Fakes.dart: implementações manuscritas dos sete contratos + ShareTextPort, com Completer, shouldFail, lastCall counters. Sem mockito.
  </behavior>
  <action>Criar test/diagnostic/. Em fakes.dart, implementar à mão (implements) NetworkSnapshotSource, LocalIpv4Source, DefaultGatewaySource, PublicIpSource, GatewayProbe, InternetProbe, ShareTextPort e um FakeCancellation-aware probe que segura um Completer, no estilo FakeCopyWriter — per D-05 e QUAL-08. Em latency_aggregator_test.dart cobrir os casos de DIAG-09 e QUAL-05 acima. Em diagnostic_session_test.dart cobrir sucesso, parcial, start concorrente, gateway ausente, cancelamento e resultado tardio per D-07, D-08, DIAG-01, DIAG-10, DIAG-12, QUAL-02, QUAL-08. Rodar os testes e confirmar que FALHAM por símbolos ausentes (RED). Não criar lib/diagnostic ainda. Não adicionar mockito, integration_test, connectivity_plus, dart_ping. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - Os três arquivos de teste existem e referenciam os tipos públicos listados em interfaces.
    - flutter test nesses arquivos falha com erro de compilação ou missing type, não com skip.
    - Nenhum import de mockito.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/latency_aggregator_test.dart test/diagnostic/diagnostic_session_test.dart</automated>
  </verify>
  <done>RED: testes QUAL-08 de domínio existem e falham porque o domínio ainda não foi implementado (D-05, DIAG-09, DIAG-12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelos, sete contratos, stubs e agregador</name>
  <files>lib/diagnostic/models/diagnostic_fact.dart, lib/diagnostic/models/diagnostic_run_state.dart, lib/diagnostic/models/latency_aggregate.dart, lib/diagnostic/models/probe_provenance.dart, lib/diagnostic/models/network_snapshot.dart, lib/diagnostic/contracts/network_snapshot_source.dart, lib/diagnostic/contracts/local_ipv4_source.dart, lib/diagnostic/contracts/default_gateway_source.dart, lib/diagnostic/contracts/public_ip_source.dart, lib/diagnostic/contracts/gateway_probe.dart, lib/diagnostic/contracts/internet_probe.dart, lib/diagnostic/contracts/diagnostic_session.dart, lib/diagnostic/contracts/share_text_port.dart, lib/diagnostic/aggregation/latency_aggregator.dart, lib/diagnostic/platform/unsupported_network_snapshot_source.dart, lib/diagnostic/local/snapshot_local_ipv4_source.dart, lib/diagnostic/gateway/snapshot_default_gateway_source.dart, lib/diagnostic/public_ip/unavailable_public_ip_source.dart, lib/diagnostic/probes/unavailable_gateway_probe.dart, lib/diagnostic/probes/unavailable_internet_probe.dart, lib/diagnostic/share/noop_share_text_port.dart</files>
  <read_first>
    - test/diagnostic/latency_aggregator_test.dart — contratos que os testes já importam
    - lib/design_system/copy_value_action.dart — abstract + impl const
    - lib/service/hash_calculator.dart — const HashResult
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Pattern 3 agregação, QUAL-05
  </read_first>
  <behavior>
    - LatencyAggregator.aggregate com as fixtures da Task 1 passa.
    - SnapshotLocalIpv4Source.fromSnapshot com localIpv4 null → unavailable; com string → success; nunca escreve 192.168.1.1.
    - SnapshotDefaultGatewaySource.fromSnapshot com gatewayIpv4 null → unavailable.
    - UnsupportedNetworkSnapshotSource.current devolve hasActiveNetwork false e campos nulos (D-04, QUAL-05).
    - UnavailablePublicIpSource / UnavailableGatewayProbe / UnavailableInternetProbe devolvem unavailable sem I/O.
    - Nenhum arquivo define class NetworkService.
  </behavior>
  <action>Implementar os tipos da seção interfaces exatamente nesses paths, imports package:tools_app/..., modelos const/final com copyWith só em DiagnosticRunState (padrão novo; não copiar mutabilidade de NetworkCalculator). LocalIpv4Source e DefaultGatewaySource são contratos Dart separados que apenas leem NetworkSnapshot — um round-trip nativo virá no plano 03-02, mas os contratos já existem per D-05. UnsupportedNetworkSnapshotSource: todos os campos indisponíveis, startWatching/stopWatching no-op. Stubs de IP/probes/share não chamam Dio, Socket nem Intent. LatencyAggregator: min/avg/max só de samples success; lista vazia de sucessos → Durations null, não Duration.zero (QUAL-05). ProbeProvenance.method nunca é ping. Não implementar DiagnosticSessionImpl nesta tarefa. Não editar telas, catálogo, Android, pubspec. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - latency_aggregator_test.dart passa.
    - Os sete papéis D-05 existem em arquivos separados; grep NetworkService em lib/diagnostic é zero.
    - Stubs não importam dart:io Socket nem package:dio.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/latency_aggregator_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub lib/diagnostic test/diagnostic</automated>
  </verify>
  <done>Contratos D-05, agregador DIAG-09 e stubs QUAL-05 compilam; testes do agregador estão verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DiagnosticSessionImpl, CancellationScope e defaults</name>
  <files>lib/diagnostic/session/cancellation_scope.dart, lib/diagnostic/session/diagnostic_session_impl.dart, lib/diagnostic/diagnostic_defaults.dart, test/diagnostic/diagnostic_session_test.dart</files>
  <read_first>
    - test/diagnostic/diagnostic_session_test.dart — casos RED da Task 1
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Pattern 2 fan-out, runId, Pitfall 1 e 4
    - lib/diagnostic/contracts/diagnostic_session.dart
  </read_first>
  <behavior>
    - Os casos da Task 1 em diagnostic_session_test.dart passam.
    - start() incrementa runId, publica running, busca snapshot, deriva localIpv4 e gateway pelos contratos 2–3, depois fan-out publicIp + gatewayProbe (só se gatewayIpv4 não-null) + internetProbe capturando erros por fato (DIAG-10).
    - Conclusão com runId != atual é descartada.
    - cancel() chama CancellationScope.cancelAll então marca cancelled; não usa Future.timeout como único abort (D-07, QUAL-02).
    - icmp fact sempre unavailable (D-10).
    - refreshSnapshotOnly atualiza snapshot/facts de plataforma sem start de probes (preparação QUAL-04).
    - dispose cancela o run atual.
    - DiagnosticDefaults.createSession() injeta stubs Unsupported/Unavailable — sem Android/Dio ainda.
  </behavior>
  <action>Implementar CancellationScope que registra closures de abort e cancela todas no cancelAll, idempotente. Implementar DiagnosticSessionImpl como ChangeNotifier ou ValueNotifier por trás de ValueListenable, sem setState, sem widgets (D-06). Construtor com os sete papéis nomeados; proibido agregar isso numa classe NetworkService (D-05). Um run por vez: se phase == running, start retorna imediatamente (DIAG-01, D-08). Após snapshot, preencher facts de transporte/INTERNET/validated/captive a partir do snapshot (valores booleanos viram success com texto wifi/cellular/vpn ou unavailable se snapshot sem rede ativa). Fan-out com Future.wait equivalent eagerError false — cada ramo captura exceção para o próprio DiagnosticFact. GatewayProbe só se snapshot.gatewayIpv4 != null; senão fato gatewayProbe unavailable e probe não chamado. Public IP falho não apaga localIpv4. Timestamps só em campos da state (DIAG-14); não gravar SharedPreferences. DiagnosticDefaults.createSession monta a impl com stubs deste plano. Ajustar testes se nomes de getters divergirem, sem enfraquecer os casos. Não criar tela nem destino. Não adicionar dio. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - diagnostic_session_test.dart e latency_aggregator_test.dart passam.
    - Não existe NetworkService. Sessão não importa Flutter material (foundation/services para ChangeNotifier ok; widgets não).
    - Defaults usam só stubs; nenhum MethodChannel nesta fatia.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/latency_aggregator_test.dart test/diagnostic/diagnostic_session_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub lib/diagnostic test/diagnostic/fakes.dart test/diagnostic/latency_aggregator_test.dart test/diagnostic/diagnostic_session_test.dart</automated>
  </verify>
  <done>Sessão testável com fakes cobre DIAG-01/10/12/14 e QUAL-02/05/08 de domínio; sete contratos injetados, sem monolito (D-05, D-06, D-07, D-08).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `lib/diagnostic/models/*.dart` | `DiagnosticFact`, `DiagnosticFactStatus`, `DiagnosticRunState`, `DiagnosticRunPhase`, `LatencyAggregate`, `ProbeProvenance`, `NetworkSnapshot` |
| `lib/diagnostic/contracts/*.dart` | Sete contratos D-05 + `ShareTextPort` |
| `lib/diagnostic/aggregation/latency_aggregator.dart` | `LatencyAggregator` |
| `lib/diagnostic/session/*.dart` | `CancellationScope`, `DiagnosticSessionImpl` |
| `lib/diagnostic/diagnostic_defaults.dart` | `DiagnosticDefaults.createSession` com stubs |
| `test/diagnostic/fakes.dart` | Fakes manuscritos QUAL-08 |

A tela e o 4º destino entram em 03-03. Snapshot Kotlin em 03-02. Dio/probes reais em 03-04/03-05.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fakes de teste → DiagnosticSessionImpl | Entrada controlada; não deve vazar para produção. |
| DiagnosticSessionImpl → adaptadores | I/O futuro atravessa aqui; runId e scope são a defesa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | DiagnosticSessionImpl | mitigate | Ignorar resultados com runId != atual; proibir NetworkService que misture fatos (D-05, D-07). |
| T-03-02 | Denial of Service | CancellationScope | mitigate | cancelAll aborta cada closure registrada; Future.timeout sozinho é proibido (QUAL-02, D-07). |
| T-03-03 | Information Disclosure | DiagnosticRunState | mitigate | Sem persistência; timestamps só em memória (DIAG-14, D-11). |
| T-03-04 | Spoofing | ProbeProvenance.method | mitigate | Enum/literais TCP connect e HTTPS; icmp sempre unavailable (D-10). |
| T-03-SC | Tampering | supply chain | accept | Nenhum pacote hosted neste plano; não editar pubspec.yaml nem pubspec.lock-old (D-14). |
</threat_model>

<verification>
1. `latency_aggregator_test.dart` e `diagnostic_session_test.dart` passam (não o diretório `test/diagnostic/` inteiro — 03-02 pode gravar testes irmãos em paralelo).
2. grep NetworkService em lib/diagnostic retorna zero matches.
3. flutter analyze --no-pub limpo em lib/diagnostic e nos três arquivos de teste deste plano.
4. git diff não inclui pubspec.lock-old, lib/screen, lib/app, android/.
</verification>

<success_criteria>
- Executor consegue start/cancel da sessão só com fakes, sem widgets.
- Sete contratos existem como tipos injetáveis separados (D-05).
- QUAL-08 de domínio (sucesso, parcial, cancel, tardio, concorrente, gateway ausente, agregação) está verde.
</success_criteria>

<output>
Create `.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-01-SUMMARY.md` when done
</output>
