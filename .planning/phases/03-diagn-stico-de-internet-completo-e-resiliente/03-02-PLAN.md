---
phase: 03-diagn-stico-de-internet-completo-e-resiliente
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/kotlin/com/example/tools_app/MainActivity.kt
  - android/app/src/main/kotlin/com/example/tools_app/NetworkSnapshotPlugin.kt
  - lib/diagnostic/platform/android_network_snapshot_source.dart
  - test/diagnostic/android_network_snapshot_source_test.dart
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03
  - DIAG-04
  - QUAL-06
must_haves:
  truths:
    - "O adapter Dart desserializa transports, INTERNET, VALIDATED, CAPTIVE_PORTAL e NOT_METERED como fatos separados, nunca um booleano isOnline (DIAG-02, D-13)."
    - "IPv4 local e gateway IPv4 vêm do mesmo mapa de snapshot e podem ser null; o código nunca preenche 192.168.1.1 (DIAG-03, DIAG-04, D-05)."
    - "O manifest main declara INTERNET e ACCESS_NETWORK_STATE; não declara localização nem ACCESS_LOCAL_NETWORK (QUAL-06, D-11)."
  artifacts:
    - path: "android/app/src/main/kotlin/com/example/tools_app/NetworkSnapshotPlugin.kt"
      provides: "Leitura ConnectivityManager/NetworkCapabilities/LinkProperties/RouteInfo"
      contains: "getSnapshot"
    - path: "lib/diagnostic/platform/android_network_snapshot_source.dart"
      provides: "AndroidNetworkSnapshotSource via MethodChannel"
      exports: ["AndroidNetworkSnapshotSource"]
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "uses-permission INTERNET e ACCESS_NETWORK_STATE no manifest main"
      contains: "ACCESS_NETWORK_STATE"
  key_links:
    - from: "lib/diagnostic/platform/android_network_snapshot_source.dart"
      to: "br.dev.rodrigopinheiro.tools_app/network_snapshot"
      via: "MethodChannel invokeMethod getSnapshot"
      pattern: "network_snapshot"
    - from: "android/app/src/main/kotlin/com/example/tools_app/MainActivity.kt"
      to: "android/app/src/main/kotlin/com/example/tools_app/NetworkSnapshotPlugin.kt"
      via: "configureFlutterEngine registra o canal"
      pattern: "configureFlutterEngine"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** executar um diagnóstico de conectividade honesto, progressivo, cancelável e resistente a falhas parciais, **so that** eu veja evidências independentes sem conclusões enganosas.

<objective>
Entregar o snapshot Android autoritativo da rede ativa (transporte, capabilities, IPv4 local, gateway da rota default) por MethodChannel + Kotlin, com permissões normais no manifest main e sem plugins connectivity_plus/network_info_plus (D-04, DIAG-02, DIAG-03, DIAG-04, QUAL-06).

Purpose: Contratos 1–3 precisam de fatos da activeNetwork; Dart NetworkInterface.list não é autoridade. Este plano pode correr em paralelo com 03-01 porque não toca lib/diagnostic/contracts nem a sessão.
Output: NetworkSnapshotPlugin.kt, canal no MainActivity, AndroidNetworkSnapshotSource, permissões main, testes de parse com fake MethodChannel.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-PATTERNS.md
@android/app/src/main/kotlin/com/example/tools_app/MainActivity.kt
@android/app/src/main/AndroidManifest.xml
@android/app/src/debug/AndroidManifest.xml
@android/app/build.gradle

<interfaces>
MainActivity.kt hoje:
- package br.dev.rodrigopinheiro.tools_app
- class MainActivity: FlutterActivity()  (arquivo em com/example/tools_app/ — NÃO mover a pasta, Pitfall 12)

Canal (RESEARCH):
- name: br.dev.rodrigopinheiro.tools_app/network_snapshot
- method getSnapshot → Map serializável
- method startWatching / stopWatching para NetworkCallback só enquanto a sessão pedir (registerDefaultNetworkCallback; unregister em stop)

Map Kotlin → Dart (chaves exactas, tipos):
- hasActiveNetwork: Boolean
- transports: List&lt;String&gt; valores wifi | cellular | vpn | ethernet
- hasInternet: Boolean
- validated: Boolean
- captive: Boolean
- notMetered: Boolean
- localIpv4: String?  (Inet4Address da LinkProperties, excluir 169.254/16 se houver outro; nunca inventar)
- gatewayIpv4: String?  (RouteInfo.isDefaultRoute && hasGateway, Inet4Address; null se não houver)
- networkHandle: Long?  (Network.networkHandle)

Dart AndroidNetworkSnapshotSource implements NetworkSnapshotSource.
Se 03-01 ainda não tiver sido mergeado no mesmo worktree, definir o implements contra os tipos em lib/diagnostic/models/network_snapshot.dart — se o arquivo ainda não existir neste branch paralelo, este plano PODE duplicar só o typedef do mapa e o parser em android_network_snapshot_source.dart desde que a assinatura current() → Future&lt;NetworkSnapshot&gt; coincida com 03-01. Preferir importar NetworkSnapshot de lib/diagnostic/models/network_snapshot.dart; se o arquivo não existir, criar o parser para Map e deixar o implements NetworkSnapshotSource para o wiring em 03-06. Na prática o execute-phase wave 1 pode correr 01 e 02 em worktrees separados — o parser deve aceitar o Map acima e construir NetworkSnapshot quando o tipo existir. Se NetworkSnapshot não existir, commitar o plugin Kotlin + o Dart source com invocação do canal e um teste de Map; 03-06 liga o implements.

Decisão do planner (discrição, evita worktree quebrado): AndroidNetworkSnapshotSource vive neste plano e importa package:tools_app/diagnostic/models/network_snapshot.dart e contracts/network_snapshot_source.dart. Wave 1 paralelo: se 03-02 executar ANTES de 03-01 no mesmo tree, o executor deve falhar o analyze e o orquestrador reexecuta 03-02 depois de 03-01. Para permitir paralelo real sem dependência de tipos, 03-02 NÃO importa os contratos de 03-01: o plugin Kotlin + testes de mapa são independentes; o Dart AndroidNetworkSnapshotSource é escrito contra Map&lt;String, dynamic&gt; helpers públicos parseNetworkSnapshotMap em android_network_snapshot_source.dart, e 03-06 faz o implements NetworkSnapshotSource. MELHOR: declarar depends implícito zero e colocar parseNetworkSnapshotMap + AndroidNetworkSnapshotSource implements NetworkSnapshotSource. Execute-phase com parallelization true usa worktrees — 03-02 worktree não tem os tipos de 03-01.

Solução travada neste plano: 03-02 só toca Android (Kotlin, Manifest, MainActivity) + um arquivo Dart de parse PURO que NÃO implements NetworkSnapshotSource ainda:

lib/diagnostic/platform/android_network_snapshot_source.dart exporta:
- const kNetworkSnapshotChannel = 'br.dev.rodrigopinheiro.tools_app/network_snapshot'
- NetworkSnapshotMapParser.parse(Map&lt;String, dynamic&gt;) → record/campos nomeados iguais ao Map (classe AndroidSnapshotFacts no mesmo arquivo: hasActiveNetwork, transports, hasInternet, validated, captive, notMetered, localIpv4, gatewayIpv4, networkHandle). Sem import de 03-01.
- class AndroidNetworkSnapshotSource com MethodChannel, método currentFacts() → Future&lt;AndroidSnapshotFacts&gt;, startWatching/stopWatching. 03-06 adapta para NetworkSnapshotSource.

Isso preserva paralelo Wave 1.

Não usar: connectivity_plus, network_info_plus, NetworkInterface.list, getActiveNetworkInfo deprecated, FLAG_INCLUDE_LOCATION_INFO, WifiInfo, SSID, BSSID, getTransportInfo, ACCESS_FINE_LOCATION, ACCESS_LOCAL_NETWORK.
Flutter CLI: C:\src\flutter\bin\flutter.bat
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Permissões main e plugin Kotlin de snapshot</name>
  <files>android/app/src/main/AndroidManifest.xml, android/app/src/main/kotlin/com/example/tools_app/NetworkSnapshotPlugin.kt, android/app/src/main/kotlin/com/example/tools_app/MainActivity.kt</files>
  <read_first>
    - android/app/src/main/AndroidManifest.xml — zero uses-permission hoje
    - android/app/src/debug/AndroidManifest.xml — INTERNET só em debug
    - android/app/src/main/kotlin/com/example/tools_app/MainActivity.kt — FlutterActivity vazio
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Pattern 1, Pitfall 8, 9, 11, 12, permissions snippet
    - android/app/build.gradle — namespace br.dev.rodrigopinheiro.tools_app, targetSdk 36
  </read_first>
  <action>No AndroidManifest MAIN, antes de application, adicionar uses-permission INTERNET e ACCESS_NETWORK_STATE somente (Pitfall 8). Não adicionar ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION, ACCESS_BACKGROUND_LOCATION, NEARBY_WIFI_DEVICES, ACCESS_LOCAL_NETWORK, CHANGE_NETWORK_STATE (QUAL-06, D-11, target 36). Manter queries PROCESS_TEXT. Criar NetworkSnapshotPlugin.kt no mesmo diretório do MainActivity (package br.dev.rodrigopinheiro.tools_app, não mover pastas — Pitfall 12). Ler getActiveNetwork + getNetworkCapabilities + getLinkProperties; transports via hasTransport WIFI/CELLULAR/VPN/ETHERNET; capabilities INTERNET, VALIDATED, CAPTIVE_PORTAL, NOT_METERED. IPv4 local: linkAddresses Inet4Address, excluir link-local 169.254.0.0/16 se existir outro; se vários, preferir o que casa com a interface da rota default IPv4; senão primeiro não-link-local; senão omitir chave/null. Gateway: routes com isDefaultRoute e hasGateway, gateway Inet4Address; senão null — NUNCA default 192.168.1.1 (DIAG-04). networkHandle para QUAL-03. Não chamar getNetworkCapabilities síncrono dentro de onAvailable (esperar onCapabilitiesChanged/onLinkPropertiesChanged). registerDefaultNetworkCallback só em startWatching; unregister em stopWatching. Sem Log de endereços em release. MainActivity override configureFlutterEngine: super + registrar canal br.dev.rodrigopinheiro.tools_app/network_snapshot métodos getSnapshot, startWatching, stopWatching. Trabalho curto na main thread. Sem EventChannel. Sem connectivity_plus / network_info_plus.</action>
  <acceptance_criteria>
    - Manifest main contém exatamente as duas permissões de rede citadas e nenhuma de localização/LAN.
    - Plugin devolve o mapa com as chaves documentadas; gateway ausente é null, não um chute.
    - MainActivity registra o canal; package permanece br.dev.rodrigopinheiro.tools_app.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat analyze --no-pub
Select-String -Path android/app/src/main/AndroidManifest.xml -Pattern "ACCESS_NETWORK_STATE" | Measure-Object | ForEach-Object { if ($_.Count -lt 1) { throw "ACCESS_NETWORK_STATE missing from main manifest" } }
$forbidden = Get-ChildItem -Path android/app/src/main -Recurse -Include *.xml,*.kt | Select-String -Pattern "ACCESS_FINE_LOCATION|ACCESS_COARSE_LOCATION|ACCESS_LOCAL_NETWORK|FLAG_INCLUDE_LOCATION_INFO"
if ($forbidden) { throw "forbidden location/LAN tokens found" }</automated>
  </verify>
  <done>Snapshot nativo Android existe com fatos DIAG-02/03/04 e privilégio mínimo QUAL-06 (D-11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parser Dart e AndroidNetworkSnapshotSource</name>
  <files>lib/diagnostic/platform/android_network_snapshot_source.dart, test/diagnostic/android_network_snapshot_source_test.dart</files>
  <read_first>
    - android/app/src/main/kotlin/com/example/tools_app/NetworkSnapshotPlugin.kt — chaves do mapa
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — MethodChannel snippet
  </read_first>
  <behavior>
    - parse de mapa completo wifi+validated+captive false+local 10.0.0.5+gateway 10.0.0.1+handle 42 preenche AndroidSnapshotFacts 1:1.
    - mapa sem localIpv4/gatewayIpv4 → esses campos null, não 192.168.1.1.
    - mapa hasActiveNetwork false → transports vazio, IPs null.
    - Fake MethodChannel getSnapshot devolve o mapa; currentFacts() propaga os campos.
    - startWatching registra o método startWatching no canal; stopWatching chama stopWatching.
  </behavior>
  <action>Criar NetworkSnapshotMapParser / AndroidSnapshotFacts e AndroidNetworkSnapshotSource usando MethodChannel(kNetworkSnapshotChannel) com invokeMethod getSnapshot / startWatching / stopWatching. Testes com tester.binding.defaultBinaryMessenger.setMockMethodCallHandler (ou TestDefaultBinaryMessengerBinding) — fakes manuscritos, sem mockito. Não implementar NetworkSnapshotSource ainda se o tipo não existir neste worktree; documentar no SUMMARY que 03-06 faz o wrap. Não chamar NetworkInterface.list. Não editar diagnostic_session, catálogo, pubspec. Não tocar pubspec.lock-old (D-14). Não pedir ACCESS_LOCAL_NETWORK; modelar permissionDenied só como campo futuro no facts Dart (enum já previsto em 03-01) — este adapter não emite permissionDenied no MVP target 36.</action>
  <acceptance_criteria>
    - Testes do parser/canal passam.
    - Código Dart não contém NetworkInterface.list nem 192.168.1.1 literal como fallback.
    - Nenhum pacote plus de conectividade adicionado.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/android_network_snapshot_source_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub lib/diagnostic/platform android/app/src/main/kotlin/com/example/tools_app</automated>
  </verify>
  <done>Dart lê o snapshot Android por canal; IPv4 local e gateway podem ser null; QUAL-06 respeitado (D-04, DIAG-02, DIAG-03, DIAG-04).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `NetworkSnapshotPlugin.kt` | Snapshot ConnectivityManager |
| `MainActivity.kt` | `configureFlutterEngine` + canal network_snapshot |
| `android_network_snapshot_source.dart` | `AndroidNetworkSnapshotSource`, `AndroidSnapshotFacts`, parser |
| `AndroidManifest.xml` (main) | `INTERNET`, `ACCESS_NETWORK_STATE` |

Wiring em DiagnosticDefaults fica em 03-06 para não conflitar com 03-01.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kotlin ConnectivityManager → Dart | Fatos de rede cruzam o MethodChannel; só tipos serializáveis. |
| App → SO | INTERNET e ACCESS_NETWORK_STATE são normal permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | NetworkSnapshotPlugin | mitigate | Não ler SSID/BSSID/transportInfo; não FLAG_INCLUDE_LOCATION_INFO (QUAL-06, D-11). |
| T-03-06 | Information Disclosure | Logcat | mitigate | Sem Log de localIpv4/gatewayIpv4/public IP em release. |
| T-03-07 | Elevation of Privilege | AndroidManifest | mitigate | Não declarar ACCESS_LOCAL_NETWORK no target 36; permissionDenied só no modelo (D-11). |
| T-03-08 | Tampering | gatewayIpv4 | mitigate | Null se RouteInfo não tiver gateway; nunca 192.168.1.1 (DIAG-04). |
| T-03-SC | Tampering | supply chain | accept | Sem pacote hosted; não connectivity_plus/network_info_plus. |
</threat_model>

<verification>
1. Manifest main tem INTERNET + ACCESS_NETWORK_STATE e zero permissões de localização/LAN.
2. Testes do parser Dart passam.
3. Kotlin não referencia SSID/BSSID/FLAG_INCLUDE_LOCATION_INFO.
</verification>

<success_criteria>
- Há uma fonte Android de snapshot da rede ativa com fatos DIAG-02/03/04.
- Localização não é solicitada (QUAL-06).
</success_criteria>

<output>
Create `.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-02-SUMMARY.md` when done
</output>
