---
phase: 03-diagn-stico-de-internet-completo-e-resiliente
plan: "05"
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-04
files_modified:
  - lib/diagnostic/probes/probe_config.dart
  - lib/diagnostic/probes/tcp_connect_probe.dart
  - lib/diagnostic/probes/dio_https_probe.dart
  - test/diagnostic/tcp_connect_probe_test.dart
  - test/diagnostic/dio_https_probe_test.dart
  - test/diagnostic/diagnostic_session_probes_test.dart
autonomous: true
requirements:
  - DIAG-06
  - DIAG-07
  - DIAG-08
  - DIAG-09
  - QUAL-02
  - QUAL-08
must_haves:
  truths:
    - "Probe de gateway é TCP connect (Socket.startConnect) porta 80 default, 4 amostras sequenciais, timeout 2s, destroy do socket; sem gateway o probe não roda (DIAG-06, DIAG-08, D-10)."
    - "Probe de internet é HTTPS GET injetável a https://www.gstatic.com/generate_204, espera 204, followRedirects false, conexão fria, 4 amostras, timeouts 5s; nunca reusa o host do ipify (DIAG-07, D-12)."
    - "Labels e provenance dizem TCP connect e HTTPS, nunca ping; copy de limitação TCP ≠ L3 down (DIAG-08, D-10)."
    - "Cancelamento aborta ConnectionTask e CancelToken; ICMP de produção não existe (QUAL-02, D-10)."
  artifacts:
    - path: "lib/diagnostic/probes/tcp_connect_probe.dart"
      provides: "TcpConnectGatewayProbe implements GatewayProbe"
      exports: ["TcpConnectGatewayProbe"]
    - path: "lib/diagnostic/probes/dio_https_probe.dart"
      provides: "DioHttpsInternetProbe implements InternetProbe"
      exports: ["DioHttpsInternetProbe"]
    - path: "lib/diagnostic/probes/probe_config.dart"
      provides: "GatewayProbeConfig e HttpsProbeConfig injetáveis"
    - path: "test/diagnostic/tcp_connect_probe_test.dart"
      provides: "QUAL-08 timeout, cancel, skip sem alvo"
  key_links:
    - from: "lib/diagnostic/probes/tcp_connect_probe.dart"
      to: "dart:io Socket.startConnect"
      via: "ConnectionTask.cancel + Socket.destroy"
      pattern: "startConnect"
    - from: "lib/diagnostic/probes/dio_https_probe.dart"
      to: "https://www.gstatic.com/generate_204"
      via: "HttpsProbeConfig.url"
      pattern: "generate_204"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** executar um diagnóstico de conectividade honesto, progressivo, cancelável e resistente a falhas parciais, **so that** eu veja evidências independentes sem conclusões enganosas.

<objective>
Implementar os probes reais de gateway (TCP connect) e internet (HTTPS 204), com provenance honesta, amostras agregáveis e cancelamento físico (DIAG-06, DIAG-07, DIAG-08, DIAG-09, D-10). Depende de dio (03-04). Não alterar DiagnosticDefaults (03-06).

Purpose: Completar o contrato 5 com I/O real testável; a sessão 03-01 já faz fan-out se os probes forem injetados nos testes.
Output: configs, TcpConnectGatewayProbe, DioHttpsInternetProbe, testes de timeout/cancel/204/3xx/gateway ausente.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-PATTERNS.md
@lib/diagnostic/contracts/gateway_probe.dart
@lib/diagnostic/contracts/internet_probe.dart
@lib/diagnostic/aggregation/latency_aggregator.dart
@lib/diagnostic/session/cancellation_scope.dart

<interfaces>
GatewayProbeConfig:
- port default 80 (injetável; A2)
- sampleCount 4 sequenciais (não paralelo)
- timeout per sample 2s
- limitations text obrigatório: Falha TCP não prova que o gateway está inalcançável em L3; roteadores costumam filtrar portas.

TcpConnectGatewayProbe:
- Socket.startConnect(InternetAddress(gatewayIpv4), port)
- Timer do timeout chama task.cancel() — não Future.timeout sozinho (Pitfall 4, D-07)
- sucesso: Stopwatch elapsed, socket.destroy() imediato
- ConnectionTask.cancel → sample cancelled
- timeout → sample timeout
- SocketException não-cancel → sample failure
- Provenance method literal TCP connect (não ping, não ICMP)
- Se a sessão passa gateway null, probe.probe NÃO é chamado (já 03-01); ainda assim o probe deve recusar host vazio com ArgumentError de programador, não inventar IP.

HttpsProbeConfig:
- url default https://www.gstatic.com/generate_204 (injetável; A1)
- expectedStatus 204
- sampleCount 4 sequenciais
- timeouts 5s
- persistentConnection false (ou Dio novo por amostra) — conexão fria (Pitfall 13)
- followRedirects false, maxRedirects 0
- metric: tempo até status (Stopwatch no client), documentado como HTTPS GET (resposta de status), conexão fria
- 3xx/200 HTML = failure, não sucesso e não captive comprovado (Pitfall 7)
- Nunca usar api.ipify.org neste probe (DIAG-05 e DIAG-07 independentes)
- User-Agent ToolsApp-Diagnostic/1.1.1

DioHttpsInternetProbe: CancelToken por amostra registrado no scope; DioException cancel → cancelled.

Não implementar ICMP; sem dart_ping; sem InetAddress.isReachable (D-10).
Testes TCP: não exigir socket real a um CPE. Injetar um typedef/callback ConnectStarter (ConnectionTask Function(InternetAddress, int)) no probe para testes, com default Socket.startConnect. Fake ConnectionTask via ServerSocket.bind(127.0.0.1, 0) é aceitável se o teste for estável e cancelável; preferir callback injetável para timeout/cancel sem rede.
Testes HTTPS: mesmo estilo de adapter fake da 03-04.
diagnostic_session_probes_test.dart (arquivo novo): snapshot sem gateway → gatewayProbe.probe count 0; snapshot com gateway + probe fake timeout → internetProbe ainda roda (DIAG-10).
Não editar tela, catálogo, DiagnosticDefaults, pubspec.lock-old.
Flutter CLI: C:\src\flutter\bin\flutter.bat
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Probe TCP do gateway</name>
  <files>lib/diagnostic/probes/probe_config.dart, lib/diagnostic/probes/tcp_connect_probe.dart, test/diagnostic/tcp_connect_probe_test.dart, test/diagnostic/diagnostic_session_probes_test.dart</files>
  <read_first>
    - lib/diagnostic/contracts/gateway_probe.dart
    - lib/diagnostic/aggregation/latency_aggregator.dart
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — TCP connect cancelável, Pattern 3, Pitfall 4 e 6
  </read_first>
  <behavior>
    - 4 sucessos 10–40 ms via ConnectStarter fake → aggregate min/avg/max só desses, plannedAttempts 4, method TCP connect.
    - ConnectStarter que não completa: timer 2s (config 50ms no teste) chama cancel → sample timeout, não hang.
    - cancel do scope no meio da 2ª amostra: amostras restantes cancelled/não executadas; ConnectionTask.cancel foi chamado.
    - Sessão com gatewayIpv4 null: FakeGatewayProbe.probeCalls == 0 e summary unavailable (QUAL-08 missing gateway).
    - Nenhuma string ping no provenance.
  </behavior>
  <action>Criar GatewayProbeConfig e TcpConnectGatewayProbe implements GatewayProbe per D-10, DIAG-06, DIAG-08, D-07. Amostras sequenciais N=4, porta 80 default, timeout 2s, destroy após connect, Timer cancela ConnectionTask. Injetar ConnectStarter para testes. Agregar com LatencyAggregator (DIAG-09). Copy de limitação L3 no provenance.limitations. Escrever testes sem mockito. Incluir o caso de sessão sem gateway em diagnostic_session_probes_test.dart usando fakes 03-01. Não usar dart_ping. Não chamar ICMP. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - Testes TCP e missing-gateway passam.
    - Provenance.method é TCP connect.
    - Socket.destroy ocorre no caminho de sucesso do starter default.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/tcp_connect_probe_test.dart test/diagnostic/diagnostic_session_probes_test.dart</automated>
  </verify>
  <done>Gateway probe é TCP connect cancelável, honesto e pulado sem alvo (DIAG-06, DIAG-08, QUAL-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Probe HTTPS de internet</name>
  <files>lib/diagnostic/probes/probe_config.dart, lib/diagnostic/probes/dio_https_probe.dart, test/diagnostic/dio_https_probe_test.dart</files>
  <read_first>
    - lib/diagnostic/public_ip/dio_public_ip_source.dart — padrão Dio+CancelToken
    - lib/diagnostic/contracts/internet_probe.dart
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — DIAG-07, Pitfall 7 e 13, A1
  </read_first>
  <behavior>
    - 4 respostas 204 → 4 sucessos, method HTTPS, url generate_204, avg não-null.
    - Status 200 com HTML ou 302 → failure, não success.
    - Timeout de 5s (config de teste curto) → timeout.
    - CancelToken no scope → cancelled.
    - Default url contém gstatic.com/generate_204 e não api.ipify.org.
  </behavior>
  <action>Criar HttpsProbeConfig e DioHttpsInternetProbe implements InternetProbe per DIAG-07, DIAG-08, D-07, D-12. GET sequencial N=4, expectedStatus 204, followRedirects false, persistentConnection false, timeouts 5s, CancelToken por amostra no scope. Métrica = Stopwatch até o status. Não reutilizar host ipify. Não rotular ping. Testes com adapter fake manuscrito, sem rede viva. Não ligar DiagnosticDefaults. Não adicionar pacotes. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - Casos 204/3xx/timeout/cancel passam.
    - Probe de internet é independente do PublicIpSource.
    - Analyze limpo em lib/diagnostic/probes.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/dio_https_probe_test.dart test/diagnostic/tcp_connect_probe_test.dart test/diagnostic/diagnostic_session_probes_test.dart test/diagnostic/latency_aggregator_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub lib/diagnostic/probes</automated>
  </verify>
  <done>Probe HTTPS cancelável, frio e substituível existe; método nomeado HTTPS (DIAG-07, DIAG-08, DIAG-09, D-10).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `GatewayProbeConfig` / `HttpsProbeConfig` | Porta, URL, N, timeouts |
| `TcpConnectGatewayProbe` | Contrato 5a produção |
| `DioHttpsInternetProbe` | Contrato 5b produção |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → gateway LAN:80 | TCP connect user-initiated a um único host (rota default). |
| App → gstatic generate_204 | GET HTTPS; terceiros veem IP e User-Agent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Information Disclosure | TCP LAN | mitigate | Um host (gateway da rota), uma porta, sem scan de faixa; user-initiated. |
| T-03-17 | Tampering | HTTPS redirect | mitigate | followRedirects false; 3xx = failure (Pitfall 7). |
| T-03-18 | Spoofing | URL do probe | mitigate | HttpsProbeConfig compilada; sem input de usuário. Não cascade. |
| T-03-19 | Denial of Service | sockets/HTTP órfãos | mitigate | ConnectionTask.cancel + CancelToken + destroy (QUAL-02, D-07). |
| T-03-20 | Tampering | label ping | mitigate | Literais TCP connect / HTTPS; teste findsNothing ping (D-10). |
| T-03-SC | Tampering | supply chain | accept | Reusa dio 03-04; sem dart_ping. |
</threat_model>

<verification>
1. Testes TCP e HTTPS passam sem rede externa.
2. Nenhuma ocorrência de dart_ping / ping como method.
3. Sessão sem gateway não chama TCP.
</verification>

<success_criteria>
- Usuário (via sessão injetada) obtém amostras TCP e HTTPS com método, alvo, timeout e denominador.
- ICMP continua fora de produção.
</success_criteria>

<output>
Create `.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-05-SUMMARY.md` when done
</output>
