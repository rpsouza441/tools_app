---
phase: 03-diagn-stico-de-internet-completo-e-resiliente
plan: "04"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/diagnostic/public_ip/public_ip_config.dart
  - lib/diagnostic/public_ip/dio_public_ip_source.dart
  - test/diagnostic/public_ip_source_test.dart
  - test/diagnostic/diagnostic_session_public_ip_test.dart
autonomous: true
requirements:
  - DIAG-05
  - QUAL-01
  - QUAL-08
must_haves:
  truths:
    - "PublicIpSource faz GET HTTPS injetável a https://api.ipify.org?format=json, valida JSON + InternetAddress.tryParse IPv4, timeouts 5s, body máx 2048 (DIAG-05, D-12)."
    - "JSON inválido, outage e timeout viram fato failure/timeout independentes — não derrubam o run nem significam sem internet (DIAG-05, DIAG-10, QUAL-01)."
    - "CancelToken é registrado no CancellationScope; DioException cancel mapeia cancelled, não failure (D-07, QUAL-02)."
    - "Não há cascade de vendors; pubspec.lock-old não é tocado (D-12, D-14)."
  artifacts:
    - path: "pubspec.yaml"
      provides: "dio: ^5.11.0"
      contains: "dio:"
    - path: "lib/diagnostic/public_ip/dio_public_ip_source.dart"
      provides: "DioPublicIpSource"
      exports: ["DioPublicIpSource"]
    - path: "lib/diagnostic/public_ip/public_ip_config.dart"
      provides: "PublicIpConfig URL/timeouts/maxBody injetáveis"
      exports: ["PublicIpConfig"]
    - path: "test/diagnostic/public_ip_source_test.dart"
      provides: "QUAL-08 JSON inválido, timeout, sucesso, body grande, cancel"
  key_links:
    - from: "lib/diagnostic/public_ip/dio_public_ip_source.dart"
      to: "package:dio/dio.dart"
      via: "Dio.get + CancelToken"
      pattern: "CancelToken"
    - from: "lib/diagnostic/public_ip/public_ip_config.dart"
      to: "https://api.ipify.org?format=json"
      via: "defaultUrl"
      pattern: "api.ipify.org"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** executar um diagnóstico de conectividade honesto, progressivo, cancelável e resistente a falhas parciais, **so that** eu veja evidências independentes sem conclusões enganosas.

<objective>
Adicionar só dio ^5.11.0 e o PublicIpSource real contra ipify, com validação, timeout, CancelToken e falha independente (DIAG-05, D-12). Não ligar ainda em DiagnosticDefaults (03-06).

Purpose: Fatia de IP público testável sem rede real, via Dio interceptor/adapter fake manuscrito. Independente da tela (03-03) e do snapshot Kotlin (03-02).
Output: dependência dio, PublicIpConfig, DioPublicIpSource, testes QUAL-08 de JSON/outage/timeout/cancel.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md
@.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-PATTERNS.md
@pubspec.yaml
@lib/diagnostic/contracts/public_ip_source.dart

<interfaces>
Instalação obrigatória (Package Legitimacy: dio Approved no 03-RESEARCH; sem checkpoint slop):
C:\src\flutter\bin\flutter.bat pub add dio:^5.11.0
Não adicionar connectivity_plus, network_info_plus, dart_ping, share_plus, permission_handler, integration_test.
Não editar pubspec.lock-old (D-14).

PublicIpConfig:
- url default https://api.ipify.org?format=json (não api64 — DIAG-05 pede IPv4)
- connectTimeout/sendTimeout/receiveTimeout = 5s
- maxBodyBytes = 2048
- userAgent ToolsApp-Diagnostic/1.1.1
- Nenhum segundo vendor; falha = fato publicIpv4 failed/unavailable

DioPublicIpSource(Dio dio, PublicIpConfig config) implements PublicIpSource:
- GET com CancelToken registrado em scope.register(() => token.cancel(...))
- status 200; se content-length ou bytes lidos &gt; 2048 → failure inválido
- data Map com ip String; InternetAddress.tryParse(ip) não-null e type IPv4; senão failure (resposta inválida)
- Provenance: method HTTPS, target api.ipify.org, portOrUrl = config.url, timeout 5s, thirdParty ipify (api.ipify.org), limitations o servidor vê o IP público; outage ≠ sem internet
- DioExceptionType.connectionTimeout/receiveTimeout/sendTimeout → status timeout
- CancelToken.isCancel → cancelled
- Outros erros de rede → failure (serviço indisponível)
- Sem setState. Sem log de IP em release.

Testes: Dio com HttpClientAdapter manuscrito ou interceptors que devolvem Response — sem mockito. Não bater na rede real nos testes.
Sessão: diagnostic_session_public_ip_test.dart monta DiagnosticSessionImpl com FakePublicIpSource failure + outros fakes success e afirma partialFailure + localIpv4 intacto (DIAG-10). Arquivo NOVO para não conflitar com diagnostic_session_test.dart.
Não editar diagnostic_defaults.dart (03-06). Não editar a tela.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar dio ^5.11.0</name>
  <files>pubspec.yaml, pubspec.lock</files>
  <read_first>
    - pubspec.yaml — pin style crypto: ^3.0.7
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Package Legitimacy Audit, dio Approved
  </read_first>
  <action>Rodar C:\src\flutter\bin\flutter.bat pub add dio:^5.11.0 per pesquisa (única dependência hosted nova). Confirmar pubspec.yaml com dio: ^5.11.0 ao lado de crypto/intl. Não pinar 5.9.2. Não adicionar outros pacotes. Não abrir nem gravar pubspec.lock-old (D-14). Version do app permanece 1.1.1+4.</action>
  <acceptance_criteria>
    - pubspec.yaml lista dio: ^5.11.0.
    - pubspec.lock (o lock principal) atualiza; pubspec.lock-old intacto.
    - Nenhum plus plugin nem dart_ping.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat pub get
Select-String -Path pubspec.yaml -Pattern "dio:" | Measure-Object | ForEach-Object { if ($_.Count -lt 1) { throw "dio missing from pubspec.yaml" } }
$forbidden = Select-String -Path pubspec.yaml -Pattern "connectivity_plus|network_info_plus|dart_ping|share_plus"
if ($forbidden) { throw "forbidden packages in pubspec.yaml" }</automated>
  </verify>
  <done>dio ^5.11.0 é a única dependência nova; lock-old intocado (D-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DioPublicIpSource e testes QUAL-08 de IP público</name>
  <files>lib/diagnostic/public_ip/public_ip_config.dart, lib/diagnostic/public_ip/dio_public_ip_source.dart, test/diagnostic/public_ip_source_test.dart, test/diagnostic/diagnostic_session_public_ip_test.dart</files>
  <read_first>
    - lib/diagnostic/contracts/public_ip_source.dart
    - lib/diagnostic/session/diagnostic_session_impl.dart
    - test/diagnostic/fakes.dart
    - .planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-RESEARCH.md — Dio CancelToken, validação ipify, A5
  </read_first>
  <behavior>
    - Body {"ip":"8.8.8.8"} status 200 → success value 8.8.8.8, provenance HTTPS + api.ipify.org.
    - Body {"ip":"not-an-ip"} ou HTML ou lista JSON → failure inválido, não exception vazando.
    - Body IPv6 → failure (DIAG-05 é IPv4).
    - Content-Length 3000 ou payload &gt; 2048 → failure.
    - Adapter que não completa até o timeout de 5s (usar config de teste com 50ms) → timeout.
    - Adapter que lança erro de conexão → failure serviço indisponível.
    - token.cancel antes da resposta → cancelled.
    - Sessão: public IP failure + snapshot/probes fake success → partialFailure, localIpv4 ainda success (DIAG-10, QUAL-08).
  </behavior>
  <action>Criar PublicIpConfig e DioPublicIpSource conforme interfaces (D-12, DIAG-05, D-07). BaseOptions: timeouts Duration 5s, followRedirects false, persistentConnection false, validateStatus aceita &lt; 600 para o caller decidir. User-Agent honesto, não fingir Chrome. Validar com jsonDecode + InternetAddress.tryParse, type IPv4. Registrar CancelToken no scope. Escrever testes com adapter/interceptor fake manuscrito — sem rede viva, sem mockito. Não cascade de vendors. Não reutilizar este host como probe de internet (DIAG-07 é outro plano). Não ligar DiagnosticDefaults. Não tocar pubspec.lock-old (D-14).</action>
  <acceptance_criteria>
    - Todos os casos de behavior passam.
    - Fonte de produção aponta só a api.ipify.org; URL é injetável.
    - Analyze limpo nos arquivos novos.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/diagnostic/public_ip_source_test.dart test/diagnostic/diagnostic_session_public_ip_test.dart test/diagnostic/diagnostic_session_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub lib/diagnostic/public_ip test/diagnostic/public_ip_source_test.dart test/diagnostic/diagnostic_session_public_ip_test.dart</automated>
  </verify>
  <done>IPv4 público HTTPS é um fato independente, cancelável e validado (DIAG-05, QUAL-01, QUAL-08, D-12).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `pubspec.yaml` | `dio: ^5.11.0` |
| `PublicIpConfig` | URL/timeouts/maxBody |
| `DioPublicIpSource` | Contrato 4 de produção |

Defaults de produção ainda usam UnavailablePublicIpSource até 03-06.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → api.ipify.org | GET HTTPS; o terceiro observa o IP público. |
| JSON de resposta → modelo | Payload não confiável. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | JSON ipify | mitigate | Tipo Map, campo ip String, tryParse IPv4, body ≤ 2048 (ASVS V5). |
| T-03-12 | Information Disclosure | ipify | accept | Exposição do IP é inerente ao serviço; divulgar hostname na UI em 03-06 (D-12, A5). Sem analytics (QUAL-07). |
| T-03-13 | Denial of Service | body bomb | mitigate | maxBodyBytes 2048 e timeouts 5s (QUAL-01). |
| T-03-14 | Spoofing | URL | mitigate | URL só em PublicIpConfig compilada; sem campo livre na UI. Sem cascade (D-12). |
| T-03-15 | Denial of Service | request órfão | mitigate | CancelToken no CancellationScope (D-07). |
| T-03-SC | Tampering | dio install | mitigate | Pacote Approved em 03-RESEARCH (publisher flutter.cn); sem checkpoint slop (sem ecossistema pub.dev no slopcheck). Não instalar homônimo PyPI. |
</threat_model>

<verification>
1. pubspec tem só dio como nova hosted dep.
2. Testes de JSON inválido, timeout, outage, cancel e falha parcial da sessão passam.
3. pubspec.lock-old unchanged.
</verification>

<success_criteria>
- IP público é medido por HTTPS cancelável contra um vendor explícito e injetável.
- Falha do ipify não apaga os outros fatos da sessão.
</success_criteria>

<output>
Create `.planning/phases/03-diagn-stico-de-internet-completo-e-resiliente/03-04-SUMMARY.md` when done
</output>
