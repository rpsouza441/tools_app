---
phase: 02-migra-o-segura-das-ferramentas-atuais
plan: "04"
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - lib/screen/hash_generator_screen.dart
  - test/screen/hash_generator_screen_test.dart
  - test/screen/tools_preservation_test.dart
autonomous: true
requirements:
  - PRES-03
  - PRES-04
must_haves:
  truths:
    - "O usuário gera hashes de abc e vê MD5 900150983cd24fb0d6963f7d28e17f72, SHA-1 a9993e364706816aba3e25717850c26c9cd0d89d, SHA-256 ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad e o SHA-512 já afirmado em test/hash_calculator_test.dart (PRES-03, D-09)."
    - "O CTA é Gerar hashes; Limpar permanece como secondary; Calcular isolado some de lib/screen (D-07)."
    - "Copiar usa TechnicalValueRow.copyWriter / CopyValueAction; o payload continua result.value; não há Clipboard.setData em lib/screen (D-08)."
    - "O aviso MD5 e SHA-1 servem para conferência, não para proteger senhas. permanece no input (PRES-04)."
    - "Rede e Armazenamento migrados continuam calculando os mesmos resultados (PRES-04, D-04, D-13)."
  artifacts:
    - path: "lib/screen/hash_generator_screen.dart"
      provides: "HashGeneratorScreen em ToolScaffold com copyWriter"
      contains: "Gerar hashes"
    - path: "test/screen/hash_generator_screen_test.dart"
      provides: "Digests de abc, erro vazio e FakeCopyWriter"
  key_links:
    - from: "lib/screen/hash_generator_screen.dart"
      to: "lib/service/hash_calculator.dart"
      via: "HashCalculator.calculate(input) only"
      pattern: "HashCalculator\\.calculate"
    - from: "lib/screen/hash_generator_screen.dart"
      to: "lib/design_system/tool_sections.dart"
      via: "TechnicalValueRow copyWriter"
      pattern: "TechnicalValueRow\\("
    - from: "lib/design_system/copy_value_action.dart"
      to: "Clipboard.setData"
      via: "ClipboardCopyWriter is the only production Clipboard.setData"
      pattern: "Clipboard.setData"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** continuar resolvendo as mesmas tarefas nas três ferramentas existentes depois da migração para a nova fundação visual, **so that** eu não perco cálculos IPv4, conversão de armazenamento nem hashes.

<objective>
Migrar HashGeneratorScreen anatomia e cópia juntos (Gerar hashes, ToolMetricLayout, TechnicalValueRow.copyWriter), então fechar o gate PRES-04 da fase: suíte completa, analyze, e grep sem Clipboard.setData / Scaffold interno / Calcular isolado em lib/screen (PRES-03, PRES-04, D-03, D-05, D-06, D-07, D-08, D-09, D-13, D-14).

Purpose: Última fatia de tela. Cópia local Clipboard.setData + SnackBar é o desvio mais forte da fundação; deve sair no mesmo plano que o chrome, não num passe extra.
Output: Hash na fundação, widget tests com FakeCopyWriter, gate de regressão das três ferramentas.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-CONTEXT.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-RESEARCH.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-PATTERNS.md
@lib/screen/hash_generator_screen.dart
@lib/service/hash_calculator.dart
@lib/design_system/copy_value_action.dart
@lib/design_system/tool_sections.dart
@test/hash_calculator_test.dart
@test/screen/screen_test_harness.dart
@test/design_system/tool_components_test.dart

<interfaces>
Keep const HashGeneratorScreen() for _buildHashGenerator. Constructor seam:

- class HashGeneratorScreen extends StatefulWidget { const HashGeneratorScreen({super.key, this.copyWriter}); final CopyValueWriter? copyWriter; }

Do not edit app_destinations.dart. Do not call package:crypto from the screen (D-09).

Keep: _calculate / _clear / dispose; field label Texto; hint ex: pacote-1.2.3.zip; error Digite ou cole um texto para calcular o hash.; warning MD5 e SHA-1 servem para conferência, não para proteger senhas.; HashCalculator.calculate(input); utf8.encode for byte counts; _characterCount = input.runes.length.

Delete: _copy; import flutter/services.dart if unused; _HashSummaryCard; _MetricChip; _HashResultTile; _MetaLabel; inner Scaffold/AppBar; ElevatedButton.icon label Calcular; Row of buttons.

Replace:
- ToolScaffold(title: 'Gerador de Hash')
- ToolInputSection: same TextField (minLines 5 maxLines 8) + warning Text
- ToolActionGroup primary ElevatedButton(onPressed: _calculate, child: Text('Gerar hashes')); secondary TextButton(onPressed: _clear, child: Text('Limpar')); icons optional; never empty onPressed; never onCancel
- When _results.isNotEmpty: ToolMetricLayout with ToolMetric(label: 'Caracteres', value: '$_characterCount'), ToolMetric(label: 'Bytes UTF-8', value: '$_byteCount'), ToolMetric(label: 'Algoritmos', value: '${_results.length}') — pass real counts, never null (null would render Indisponível)
- Each HashResult: TechnicalValueRow(label: result.algorithm, value: result.value, metadata: '${result.bits} bits · ${result.strength} · ${result.value.length} hex', copyWriter: widget.copyWriter ?? const ClipboardCopyWriter())
- Copied payload remains result.value. SnackBar becomes {Rótulo} copiado via CopyValueAction (MD5 → MD5 copiado). Do not keep a parallel _copy path.

abc digests (do not edit test/hash_calculator_test.dart):
- MD5 900150983cd24fb0d6963f7d28e17f72
- SHA-1 a9993e364706816aba3e25717850c26c9cd0d89d
- SHA-256 ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad
- SHA-512 ddaf35a193617abacc417349ae20413112e6fa4e89a97ea20a9eeee64b55d39a2192992a274fc1a836ba3c23a3feebbd454d4423643ce80e2a9ac94fa54ca49f

Phase regression gate (D-13) after GREEN widget tests:
- Full flutter test --no-pub
- flutter analyze --no-pub
- rg Clipboard.setData in lib/screen must match nothing (only ClipboardCopyWriter in copy_value_action.dart may call it)
- rg return Scaffold( and AppBar( in lib/screen must match nothing
- rg Text('Calcular') in lib/screen must match nothing (leave test/design_system/tool_components_test.dart sample Calcular alone)
- Do not regenerate test/design_system/goldens/ unless a shared primitive changed (it must not)
- Do not touch pubspec.lock-old; do not add Diagnóstico screens/packages/routers (D-11, D-12)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Hash widget tests with FakeCopyWriter</name>
  <files>test/screen/hash_generator_screen_test.dart</files>
  <read_first>
    - test/screen/screen_test_harness.dart — wrapScreen and FakeCopyWriter
    - lib/screen/hash_generator_screen.dart — _calculate, _copy, warning string
    - test/hash_calculator_test.dart — abc digest oracles (do not edit)
    - lib/design_system/copy_value_action.dart — CopyValueAction / SnackBar contract already tested; do not reimplement
  </read_first>
  <behavior>
    - wrapScreen(HashGeneratorScreen(copyWriter: writer)); enter Texto abc; tap Gerar hashes; pumpAndSettle.
    - find.text each of the four abc digests listed in interfaces (SHA-512 may be asserted with find.textContaining of the first 32 hex chars plus a second containing the remainder if a single find.text is unwieldy).
    - find.text('Calcular') findsNothing; find.text('Cancelar') findsNothing; find.text('Gerar hashes') findsWidgets; find.text('Limpar') findsWidgets.
    - Warning MD5 e SHA-1 servem para conferência, não para proteger senhas. findsOneWidget.
    - ToolMetric labels Caracteres, Bytes UTF-8, Algoritmos with values 3, 3, 4 for abc.
    - tap find.byType(CopyValueAction).first; writer.lastValue == 900150983cd24fb0d6963f7d28e17f72 (MD5); do not use the host clipboard.
    - Empty submit: Digite ou cole um texto para calcular o hash.
    - find.byType(AppBar) findsNothing; ToolScaffold present; title Gerador de Hash.
  </behavior>
  <action>Create hash_generator_screen_test.dart. Tests MUST be RED because Gerar hashes, ToolScaffold and CopyValueAction are not in the production screen yet. Do not test Clipboard.setData. Do not edit hash_calculator_test.dart. Do not make a tautological-only ToolScaffold assert without digests (D-13).</action>
  <acceptance_criteria>
    - RED for missing Gerar hashes / CopyValueAction / split digest rows.
    - FakeCopyWriter oracle is the MD5 of abc, not a SnackBar string.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/hash_generator_screen_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before Hash ToolScaffold+copy migration' }</automated>
  </verify>
  <done>Failing tests lock PRES-03 digests, D-07 CTA, D-08 copy payload, and the MD5/SHA-1 warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap HashGeneratorScreen and replace _copy with TechnicalValueRow</name>
  <files>lib/screen/hash_generator_screen.dart, test/screen/tools_preservation_test.dart</files>
  <read_first>
    - test/screen/hash_generator_screen_test.dart — RED contract
    - lib/screen/hash_generator_screen.dart — _calculate/_clear and widgets to delete
    - lib/design_system/tool_sections.dart — TechnicalValueRow.copyWriter slot
    - test/screen/tools_preservation_test.dart — Hash group still expects Calcular and AppBar
  </read_first>
  <action>Migrate Hash anatomy and copy together (D-05, D-06, D-07, D-08). Delete _copy and the private _HashSummaryCard / _MetricChip / _HashResultTile / _MetaLabel widgets after replacement. Wire TechnicalValueRow.copyWriter as specified; do not keep Clipboard.setData plus CopyValueAction (two SnackBars). Keep HashCalculator.calculate as the only digest path (D-09). Optional copyWriter must not break const HashGeneratorScreen(). Update tools_preservation_test Hash group: heading Gerador de Hash, CTA Gerar hashes, warning still present; Rede stays Calcular rede; Armazenamento stays Analisar capacidade (PRES-04). Do not edit the other two production screens except if a compile error proves a shared import issue (there should be none). Do not edit services, goldens, tool_components_test sample Calcular, or pubspec.lock-old. Do not add Diagnóstico.</action>
  <acceptance_criteria>
    - hash_generator_screen_test.dart GREEN including FakeCopyWriter MD5 payload.
    - hash_generator_screen.dart contains Gerar hashes and TechnicalValueRow and does not contain Clipboard.setData, _copy, Text('Calcular'), or return Scaffold(.
    - Warning string unchanged.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/hash_generator_screen_test.dart test/screen/tools_preservation_test.dart test/screen/network_calculator_screen_test.dart test/screen/data_converter_screen_test.dart test/hash_calculator_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>PRES-03 holds with CopyValueAction; the three preservation groups all use the new CTAs (PRES-04).</done>
</task>

<task type="auto">
  <name>Task 3: Full PRES-04 regression gate</name>
  <files>lib/screen/hash_generator_screen.dart, lib/screen/network_calculator_screen.dart, lib/screen/data_converter_screen.dart</files>
  <read_first>
    - lib/screen/hash_generator_screen.dart
    - lib/screen/network_calculator_screen.dart
    - lib/screen/data_converter_screen.dart
    - lib/design_system/copy_value_action.dart — ClipboardCopyWriter remains the only Clipboard.setData
    - test/design_system/goldens/ — must not gain new PNGs in this plan
  </read_first>
  <action>Run the full suite and grep gates for the phase (D-13, PRES-04). Clipboard.setData must not appear under lib/screen (scan that directory only; copy_value_action.dart is allowed). No production screen may return Scaffold, own an AppBar, paint isolated Text('Calcular'), or pass onCancel to ToolActionGroup. Confirm the three CTAs Calcular rede, Analisar capacidade, Gerar hashes exist. Confirm services were not edited by checking that test/network_calculator_test.dart, test/data_converter_test.dart and test/hash_calculator_test.dart still pass with their original asserts. Do not rewrite foundation goldens. Do not start Phase 3. If any gate fails, fix only production screens/tests this phase owns.</action>
  <acceptance_criteria>
    - flutter test --no-pub exits 0.
    - flutter analyze --no-pub is clean.
    - lib/screen has zero Clipboard.setData, zero inner Scaffold/AppBar, zero isolated Calcular, zero onCancel.
    - test/design_system/goldens/ is unchanged in git status for this plan.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Clipboard.setData" lib/screen --glob "*.dart"; if ($LASTEXITCODE -eq 0) { throw 'Clipboard.setData still in lib/screen' }
rg -n "return Scaffold\(|AppBar\(|Text\('Calcular'\)|onCancel:" lib/screen --glob "*.dart"; if ($LASTEXITCODE -eq 0) { throw 'Forbidden chrome/CTA still in lib/screen' }
rg -n "Calcular rede" lib/screen/network_calculator_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'Missing Calcular rede' }
rg -n "Analisar capacidade" lib/screen/data_converter_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'Missing Analisar capacidade' }
rg -n "Gerar hashes" lib/screen/hash_generator_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'Missing Gerar hashes' }
C:\src\flutter\bin\flutter.bat test --no-pub
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>Phase 2 goal is observable: all three tools solve the same tasks on the new foundation with no leftover local copy/chrome (PRES-01..PRES-04, D-01, D-13).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `lib/screen/hash_generator_screen.dart` | ToolScaffold wrap; optional `copyWriter`; CTA `Gerar hashes`; no `_copy` |
| `test/screen/hash_generator_screen_test.dart` | PRES-03 widget + FakeCopyWriter regression |
| `test/screen/tools_preservation_test.dart` | All three groups on new CTAs |
| `lib/screen/network_calculator_screen.dart` | (from 02-02) Rede wrap |
| `lib/screen/data_converter_screen.dart` | (from 02-03) Armazenamento wrap |
| `test/screen/screen_test_harness.dart` | (from 02-01) `wrapScreen` / `FakeCopyWriter` |

Phase 2 does not produce Diagnóstico screens, new packages, routers, or foundation golden rewrites.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Texto field → HashCalculator | Arbitrary user text; output is display-only integrity hashes, not authentication. |
| CopyValueAction → OS clipboard | Digest hex leaves the app for the session only. |
| Warning copy → user belief | MD5/SHA-1 must not be presented as password hashing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | Hash input warning | mitigate | Keep MD5 e SHA-1 servem para conferência, não para proteger senhas. on the input section (ASVS V6 display-only; D-09). |
| T-02-12 | Tampering | digest computation | mitigate | Call only HashCalculator.calculate; do not hand-roll md5/sha in the screen; abc oracles stay in unit tests (D-09). |
| T-02-13 | Information Disclosure | clipboard | mitigate | Copy result.value via CopyValueAction; delete _copy/Clipboard.setData from lib/screen; no analytics (D-08, D-11). |
| T-02-14 | Spoofing | dual copy paths | mitigate | Do not leave _copy alongside CopyValueAction; grep gate forbids Clipboard.setData under lib/screen. |
| T-02-SC | Tampering | package supply chain | accept | crypto already pinned; no new packages; do not edit pubspec.lock-old (D-11, D-12). |
</threat_model>

<verification>
1. Hash widget tests show four abc digests, empty-field error, warning, and FakeCopyWriter MD5 payload.
2. Full flutter test --no-pub and analyze --no-pub pass.
3. lib/screen grep gates: no Clipboard.setData, no inner Scaffold/AppBar, no isolated Calcular, no onCancel.
4. Foundation goldens directory unmodified. No Phase 3 files added.
</verification>

<success_criteria>
- User generates the same MD5/SHA-1/SHA-256/SHA-512 values after the wrap (PRES-03).
- User still uses Rede and Armazenamento with their migrated CTAs (PRES-04).
- Production copy confirmation is CopyValueAction ({Rótulo} copiado), not a local SnackBar.
- The phase goal is true: the three existing tools still solve the same tasks on the new visual foundation (D-01).
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-04-SUMMARY.md` when done
</output>
