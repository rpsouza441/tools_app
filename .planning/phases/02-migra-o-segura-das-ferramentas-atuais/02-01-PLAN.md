---
phase: 02-migra-o-segura-das-ferramentas-atuais
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/app/app_shell_test.dart
  - test/screen/screen_test_harness.dart
  - test/screen/tools_preservation_test.dart
autonomous: true
requirements:
  - PRES-04
must_haves:
  truths:
    - "O usuário abre Rede, Armazenamento e Hash pelo App de produção e ainda vê as três ferramentas utilizáveis com chrome atual (PRES-04, D-04)."
    - "Testes que montam appDestinations passam lightTheme/AppTokens antes de qualquer tela largar o Scaffold interno (D-05, D-13)."
    - "Nenhuma tela de produção, serviço, CTA, golden da fundação ou pubspec muda neste plano (D-09, D-11, D-12)."
  artifacts:
    - path: "test/screen/screen_test_harness.dart"
      provides: "wrapScreen com lightTheme e FakeCopyWriter para testes isolados das telas"
      contains: "wrapScreen"
    - path: "test/screen/tools_preservation_test.dart"
      provides: "Fumaça PRES-04 das três ferramentas via const App()"
      contains: "Analisar Capacidade"
    - path: "test/app/app_shell_test.dart"
      provides: "Pumps de appDestinations com theme: lightTheme"
  key_links:
    - from: "test/app/app_shell_test.dart"
      to: "lib/theme/theme.dart"
      via: "MaterialApp.theme recebe lightTheme nos pumps de appDestinations"
      pattern: "theme:\\s*lightTheme"
    - from: "test/screen/tools_preservation_test.dart"
      to: "lib/main.dart"
      via: "pumpWidget(const App())"
      pattern: "const App\\(\\)"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** continuar resolvendo as mesmas tarefas nas três ferramentas existentes depois da migração para a nova fundação visual, **so that** eu não perco cálculos IPv4, conversão de armazenamento nem hashes.

<objective>
Preparar o harness de tema e a fumaça PRES-04 para que AppShell/produção suportem AppTokens antes da primeira tela largar o Scaffold interno, sem migrar chrome de produção (D-01, D-02, D-04, D-12, D-14).

Purpose: ToolScaffold faz Theme.extension&lt;AppTokens&gt;()!; pumps atuais de appDestinations sem lightTheme quebram no plano seguinte. PRES-04 exige prova de que as três ferramentas continuam utilizáveis neste incremento.
Output: wrapScreen/FakeCopyWriter, tools_preservation_test.dart, app_shell_test com lightTheme nos quatro pumps de appDestinations.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-CONTEXT.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-RESEARCH.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-PATTERNS.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@lib/main.dart
@lib/theme/theme.dart
@test/app/app_shell_test.dart
@test/design_system/tool_components_test.dart
@test/design_system/accessibility_test.dart

<interfaces>
From lib/theme/theme.dart:
- final ThemeData lightTheme — includes extensions: const &lt;ThemeExtension&gt;[AppTokens()]

From lib/main.dart:
- class App extends StatelessWidget — MaterialApp(theme: lightTheme, darkTheme: darkTheme, home: AppShell(destinations: appDestinations))
- const App() already supplies tokens; do not wrap App() in an extra Scaffold

From lib/design_system/tool_scaffold.dart:
- ToolScaffold.build reads Theme.of(context).extension&lt;AppTokens&gt;()! — null-check crash without lightTheme

From lib/design_system/copy_value_action.dart:
- abstract class CopyValueWriter { Future&lt;void&gt; write(String value); }
- class ClipboardCopyWriter implements CopyValueWriter { const ClipboardCopyWriter(); }

From test/design_system/tool_components_test.dart (copy analog, do not edit this file):
- class FakeCopyWriter implements CopyValueWriter with lastValue, callCount, shouldThrow
- Widget _wrap(Widget child, {double? width}) uses MaterialApp(theme: lightTheme, home: Scaffold(body: child))

From test/app/app_shell_test.dart — production pumps that MUST gain theme: lightTheme (D-13):
- group AppShell boundary adaptation: 599px NavigationBar, 600px collapsed rail, 840px extended rail — MaterialApp(home: AppShell(destinations: appDestinations))
- group AppShell semanticLabel consumption: production collapsed rail at 600px — same missing theme
Leave _pumpShell and fake-destination groups unchanged (they do not embed ToolScaffold).
Happy path already uses const App() — do not add a second Scaffold.

Current production chrome this plan must KEEP asserting (D-04, D-07 not applied yet):
- Rede AppBar title Calculadora de Rede; CTA Text('Calcular'); Limpar
- Armazenamento AppBar Conversor de Armazenamento; CTA Text('Analisar Capacidade')
- Hash AppBar Gerador de Hash; CTA Text('Calcular')
Catalog compact labels Rede / Armazenamento / Hash and tooltips Calculadora de Rede / Conversor de Dados / Gerador de Hash stay unchanged (do not edit app_destinations.dart).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wrapScreen harness and PRES-04 three-tool smoke</name>
  <files>test/screen/screen_test_harness.dart, test/screen/tools_preservation_test.dart</files>
  <read_first>
    - test/design_system/tool_components_test.dart — FakeCopyWriter and _wrap analog
    - test/app/app_shell_test.dart — Happy path group (const App(), 360x800, physicalSize + addTearDown)
    - lib/main.dart — App already sets lightTheme
    - lib/screen/network_calculator_screen.dart — AppBar Calculadora de Rede, Text('Calcular')
    - lib/screen/data_converter_screen.dart — AppBar Conversor de Armazenamento, Text('Analisar Capacidade')
    - lib/screen/hash_generator_screen.dart — AppBar Gerador de Hash, Text('Calcular')
  </read_first>
  <action>Create test/screen/ (directory does not exist). In screen_test_harness.dart export a public wrapScreen(Widget child) that returns MaterialApp(theme: lightTheme, home: Scaffold(body: child)) and a public FakeCopyWriter implementing CopyValueWriter with lastValue, callCount and shouldThrow matching the analog in tool_components_test.dart. Do not import or edit tool_components_test.dart. Create tools_preservation_test.dart that pumps const App() at 360x800 DPR 1 with addTearDown reset, then per D-04/PRES-04: (1) on Rede, find descendant AppBar matching Calculadora de Rede, find.text('Calcular'), find.text('Limpar'), TextField labels Endereço de IP and Máscara de Sub-Rede ou CIDR; (2) tap Armazenamento, find AppBar Conversor de Armazenamento and Analisar Capacidade and Valor Anunciado; (3) tap Hash, find AppBar Gerador de Hash, field label Texto, warning MD5 e SHA-1 servem para conferência, não para proteger senhas., and Calcular. Use tester.view.physicalSize and do not use deprecated TestWindow APIs. Do not change any file under lib/. Do not add Diagnóstico destinations. Do not touch pubspec.lock-old (D-12). Do not regenerate test/design_system/goldens/ (D-13).</action>
  <acceptance_criteria>
    - wrapScreen and FakeCopyWriter exist and compile against CopyValueWriter.
    - The smoke test proves all three current screens are reachable and show their current titles and CTAs via const App().
    - No production Dart file under lib/ is modified.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/tools_preservation_test.dart</automated>
  </verify>
  <done>PRES-04 has an automated three-tool smoke using the live App, and later screen tests have a shared lightTheme wrapper (D-04, D-13).</done>
</task>

<task type="auto">
  <name>Task 2: Supply lightTheme on every AppShell appDestinations pump</name>
  <files>test/app/app_shell_test.dart</files>
  <read_first>
    - test/app/app_shell_test.dart — four MaterialApp pumps of AppShell(destinations: appDestinations) without theme
    - test/design_system/accessibility_test.dart — _buildPublicFixture(theme: lightTheme) analog
    - lib/theme/theme.dart — lightTheme export
  </read_first>
  <action>Import package:tools_app/theme/theme.dart. On every pumpWidget that uses AppShell(destinations: appDestinations) — the three boundary tests at 599/600/840 and the production collapsed-rail semantic test at 600px — set MaterialApp(theme: lightTheme, home: AppShell(destinations: appDestinations)) per D-13 and 02-RESEARCH pitfall 2. Do not change _pumpShell (fake destinations). Do not change Happy path selectors (still Calcular and Endereço de Rede: 192.168.1.0 blob; those update in 02-02 per D-10). Do not change catalog tooltip assertions. Do not edit lib/app/app_shell.dart or lib/app/app_destinations.dart (D-03). After the wrap, run the AppShell file plus the three service oracles and analyze to prove PRES-04: Rede/Armazenamento/Hash remain usable with current chrome (D-04, D-09).</action>
  <acceptance_criteria>
    - All four appDestinations pumps pass theme: lightTheme.
    - Fake-destination tests still omit tokens.
    - Happy path still uses isolated Calcular and the concatenated result finder (not yet migrated).
    - Service value asserts are untouched.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/app/app_shell_test.dart test/screen/tools_preservation_test.dart test/network_calculator_test.dart test/data_converter_test.dart test/hash_calculator_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>AppShell production tests are AppTokens-safe and the three tools still work with their current chrome (PRES-04).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `test/screen/screen_test_harness.dart` | `wrapScreen`, `FakeCopyWriter` |
| `test/screen/tools_preservation_test.dart` | PRES-04 smoke for Rede / Armazenamento / Hash |
| `test/app/app_shell_test.dart` | `lightTheme` on production `appDestinations` pumps |

This plan does not migrate production screens. Rede/Armazenamento/Hash chrome changes belong to 02-02, 02-03 and 02-04.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget test fixture → production destinations | Tests that mount appDestinations instantiate real screens; missing AppTokens crashes ToolScaffold later. |
| Test harness → clipboard abstraction | FakeCopyWriter must not write the OS clipboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | AppShell tests without AppTokens | mitigate | Pass lightTheme on every appDestinations pump so extension&lt;AppTokens&gt;()! cannot throw (D-13). |
| T-02-02 | Tampering | PRES-04 smoke vs production | mitigate | Smoke uses const App() and current titles/CTAs; do not stub pageBuilders or add parallel screens (D-03, D-04). |
| T-02-03 | Information Disclosure | FakeCopyWriter | mitigate | Record lastValue in memory only; never call Clipboard.setData in the harness. |
| T-02-SC | Tampering | package supply chain | accept | No package-manager install; do not edit pubspec.yaml or pubspec.lock-old (D-11, D-12). |
</threat_model>

<verification>
1. tools_preservation_test.dart opens Rede, Armazenamento and Hash with current AppBar titles and CTAs.
2. app_shell_test.dart production pumps include theme: lightTheme; fake pumps do not.
3. Service tests and flutter analyze --no-pub stay green.
4. git diff -- lib/ is empty. test/design_system/goldens/ is untouched.
</verification>

<success_criteria>
- A user can still open all three tools in the real App after this increment (PRES-04).
- The next plan can drop NetworkCalculatorScreen's inner Scaffold without crashing AppShell boundary tests.
- No CTA, service, destination id, or foundation golden changes.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-01-SUMMARY.md` when done
</output>
