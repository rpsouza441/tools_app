---
phase: 02-migra-o-segura-das-ferramentas-atuais
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/screen/network_calculator_screen.dart
  - test/screen/network_calculator_screen_test.dart
  - test/screen/tools_preservation_test.dart
  - test/app/app_shell_test.dart
autonomous: true
requirements:
  - PRES-01
  - PRES-04
must_haves:
  truths:
    - "O usuário calcula 192.168.1.10 /24 e vê o valor 192.168.1.0 como Endereço de Rede, não o blob concatenado (PRES-01, D-07, D-09, D-10)."
    - "O CTA visível na Calculadora de Rede é Calcular rede; Limpar usa ToolActionGroup.secondary e nunca pinta Cancelar (D-07)."
    - "A tela não tem Scaffold/AppBar internos; o título Calculadora de Rede vive no ToolScaffold (D-05, D-06)."
    - "Armazenamento e Hash continuam utilizáveis com chrome ainda não migrado (PRES-04, D-04)."
  artifacts:
    - path: "lib/screen/network_calculator_screen.dart"
      provides: "NetworkCalculatorScreen em ToolScaffold com TechnicalValueRow"
      contains: "Calcular rede"
    - path: "test/screen/network_calculator_screen_test.dart"
      provides: "Happy path, erros de campo e cópia do endereço de rede"
    - path: "test/app/app_shell_test.dart"
      provides: "Happy path atualizado para Calcular rede e valor 192.168.1.0"
  key_links:
    - from: "lib/screen/network_calculator_screen.dart"
      to: "lib/design_system/tool_scaffold.dart"
      via: "build retorna ToolScaffold, não Scaffold"
      pattern: "ToolScaffold\\("
    - from: "lib/screen/network_calculator_screen.dart"
      to: "lib/service/network_calculator.dart"
      via: "_calculate continua chamando NetworkCalculator"
      pattern: "NetworkCalculator\\("
    - from: "test/app/app_shell_test.dart"
      to: "lib/screen/network_calculator_screen.dart"
      via: "tap Calcular rede e find.text('192.168.1.0')"
      pattern: "Calcular rede"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** continuar resolvendo as mesmas tarefas nas três ferramentas existentes depois da migração para a nova fundação visual, **so that** eu não perco cálculos IPv4, conversão de armazenamento nem hashes.

<objective>
Migrar somente NetworkCalculatorScreen para a fundação Phase 1 (ToolScaffold, seções, Calcular rede, TechnicalValueRow/cópia) preservando resultados IPv4 e deixando Armazenamento/Hash utilizáveis (PRES-01, PRES-04, D-03, D-05, D-06, D-07, D-08, D-09, D-10, D-14).

Purpose: Primeira fatia de tela real. O happy path do AppShell já exercita Rede; seletores e finders devem atualizar neste mesmo plano.
Output: Tela Rede sem Scaffold interno, testes de widget, AppShell happy path verde, outras duas telas intactas.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-CONTEXT.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-RESEARCH.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-PATTERNS.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-UI-SPEC.md
@.planning/phases/01-contrato-visual-e-funda-o-adaptativa/01-UI-SPEC.md
@lib/screen/network_calculator_screen.dart
@lib/design_system/tool_scaffold.dart
@lib/design_system/tool_sections.dart
@lib/design_system/copy_value_action.dart
@test/screen/screen_test_harness.dart
@test/app/app_shell_test.dart
@test/network_calculator_test.dart

<interfaces>
Keep const NetworkCalculatorScreen() so app_destinations.dart _buildNetworkCalculator stays valid (D-03). Optional seam:

- class NetworkCalculatorScreen extends StatefulWidget { const NetworkCalculatorScreen({super.key, this.copyWriter}); final CopyValueWriter? copyWriter; }

Do not edit lib/app/app_destinations.dart. pageBuilder must remain const NetworkCalculatorScreen().

Composition (gallery analog; production must wire real callbacks, never onPressed: () {}):
- return ToolScaffold(title: 'Calculadora de Rede', children: [ToolInputSection with the two existing TextFields, SizedBox height 24, ToolActionGroup, results])
- ToolActionGroup primary: ElevatedButton(onPressed: _calculate, child: Text('Calcular rede'))
- ToolActionGroup secondary: TextButton(onPressed: _clearFields, child: Text('Limpar'))
- Never pass onCancel (it always paints Cancelar)

Structured results (delete the concatenated _result blob as UI; keep the six computed strings from NetworkCalculator):
- TechnicalValueRow label Endereço de Rede, value networkAddress
- TechnicalValueRow label Faixa de IPs, value ipRange
- TechnicalValueRow label Endereço de Broadcast, value broadcastAddress
- TechnicalValueRow label Máscara de Sub-rede, value calculator.subnetMask
- TechnicalValueRow label CIDR, value /${calculator.cidr}
- ToolMetric label Hosts utilizáveis, value '$usableHosts'
- copyWriter: widget.copyWriter ?? const ClipboardCopyWriter() on each TechnicalValueRow
- Copied payload is the value string (192.168.1.0), not Endereço de Rede: 192.168.1.0

Character-exact field errors (stay on InputDecoration.errorText; do not replace with ToolStatusPanel.empty/loading/offline):
- Formato de IP inválido (ex: 192.168.1.1).
- Insira uma máscara de sub-rede ou um CIDR.
- Máscara de sub-rede inválida.
- Valor de CIDR inválido (0-32).
Catch FormatException text: Erro: Formato de entrada inválido. Verifique os valores inseridos.
Generic catch: Erro inesperado: ${e.toString()}
Show catch-all as body Text under the action/result region, not as a diagnostic status panel.

Delete: inner Scaffold, AppBar, LayoutBuilder maxWidth &gt; 600 Row flex 7/3, local Padding 16, compact Row width * 0.6, Text('Calcular'), concatenated _result Text blob.

Keep: isValidIp / isValidSubnetMask / isValidCidr / NetworkCalculator calls / dispose of both controllers. Do not extract a controller class. Do not edit lib/service/network_calculator.dart or lib/utils/network_utils.dart (D-09).

Service oracles (do not edit test/network_calculator_test.dart asserts): 192.168.1.10 cidr 24 → network 192.168.1.0, broadcast 192.168.1.255, mask 255.255.255.0, range 192.168.1.1 - 192.168.1.254, hosts 254.

AppShell happy path updates in this same plan (D-10):
- find.text('Calculadora de Rede') findsWidgets (heading + tooltip)
- descendant of AppBar matching Calculadora de Rede → findsNothing
- tap Calcular rede (not Calcular)
- expect find.text('Endereço de Rede') and find.text('192.168.1.0'); if SelectableText misses find.text, allow find.textContaining('192.168.1.0', findRichText: true) (research A1)
- IndexedStack: tap Armazenamento then Rede → 192.168.1.0 still visible
- expect find.text('Calcular') findsNothing while Rede is the visible page (Hash still has Calcular offstage; use skipOffstage default so only the visible Rede CTA is considered, or assert find.text('Calcular rede') and that Rede's ElevatedButton child is not Calcular)
- Compact-bar test: drop the AppBar descendant finder the same way; keep tooltips Calculadora de Rede / Conversor de Dados / Gerador de Hash
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Rede widget tests (happy path, field errors, copy)</name>
  <files>test/screen/network_calculator_screen_test.dart</files>
  <read_first>
    - test/screen/screen_test_harness.dart — wrapScreen and FakeCopyWriter from 02-01
    - lib/screen/network_calculator_screen.dart — current _calculate validators and blob
    - test/app/app_shell_test.dart — current happy path oracles
    - test/network_calculator_test.dart — value oracles (do not edit)
    - lib/design_system/tool_sections.dart — TechnicalValueRow / ToolActionGroup
  </read_first>
  <behavior>
    - wrapScreen(const NetworkCalculatorScreen()) at default size; no production AppBar (find.byType(AppBar) findsNothing); find.byType(ToolScaffold) findsOneWidget; title Calculadora de Rede visible.
    - Enter 192.168.1.10 in Endereço de IP, 24 in Máscara de Sub-Rede ou CIDR, tap Calcular rede, pumpAndSettle: find.text('192.168.1.0'), find.text('Endereço de Rede'), find.text('255.255.255.0'), find.text('192.168.1.1 - 192.168.1.254'), find.text('254'), find.text('Calcular') findsNothing, find.text('Cancelar') findsNothing.
    - Limpar then tap Calcular rede: Formato de IP inválido (ex: 192.168.1.1).
    - Valid IP, empty mask: Insira uma máscara de sub-rede ou um CIDR.
    - Valid IP, invalid dotted mask that fails isValidSubnetMask: Máscara de sub-rede inválida.
    - Valid IP, CIDR 33: Valor de CIDR inválido (0-32).
    - wrapScreen(NetworkCalculatorScreen(copyWriter: writer)); after happy path, tap first CopyValueAction; writer.lastValue == 192.168.1.0 (payload is the IP, not the old blob prefix).
  </behavior>
  <action>Create test/screen/network_calculator_screen_test.dart using wrapScreen from screen_test_harness.dart. Tests MUST fail before the screen migration because Calcular rede, ToolScaffold and split rows do not exist yet. Do not weaken asserts to match the current blob. Do not edit test/network_calculator_test.dart. Do not assert only find.byType(ToolScaffold) without IP/error oracles (D-13). Isolated widget tests supply the throwaway Scaffold via wrapScreen, not inside production.</action>
  <acceptance_criteria>
    - Targeted test file is RED for missing Calcular rede / ToolScaffold / split 192.168.1.0 row, not because of a malformed finder.
    - All four character-exact field errors are expressed as expectations.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/network_calculator_screen_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before Rede ToolScaffold migration' }</automated>
  </verify>
  <done>Failing tests lock PRES-01 results and D-07 CTA before production chrome changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap NetworkCalculatorScreen and update AppShell selectors</name>
  <files>lib/screen/network_calculator_screen.dart, test/app/app_shell_test.dart, test/screen/tools_preservation_test.dart</files>
  <read_first>
    - test/screen/network_calculator_screen_test.dart — RED contract from Task 1
    - lib/screen/network_calculator_screen.dart — keep _calculate/_clearFields/dispose logic
    - test/design_system/design_system_gallery.dart — ToolScaffold composition analog
    - lib/design_system/tool_sections.dart — onCancel paints Cancelar; secondary is Limpar
    - test/app/app_shell_test.dart — Happy path and compact-bar AppBar finders
    - test/screen/tools_preservation_test.dart — Rede group still expects Calcular and AppBar
  </read_first>
  <action>Replace the production build of NetworkCalculatorScreen with ToolScaffold as specified in interfaces (D-05, D-06, D-07, D-08). Keep _calculate validation and NetworkCalculator usage character-exact (D-09); store structured fields instead of one _result String so labels and values are separate widgets. Add optional copyWriter without breaking const NetworkCalculatorScreen(). Delete inner Scaffold/AppBar and the local &gt; 600 Row. Update AppShell happy path and compact-bar AppBar descendant finder in the SAME plan (D-10): Calcular → Calcular rede; split Endereço de Rede and 192.168.1.0; AppBar title finder findsNothing; heading Calculadora de Rede remains. Update tools_preservation_test Rede group to heading + Calcular rede while Armazenamento still expects Analisar Capacidade and Hash still expects Calcular plus Gerador de Hash AppBar (PRES-04, D-04). Do not migrate data_converter_screen.dart or hash_generator_screen.dart. Do not use ToolActionGroup.onCancel. Do not add Diagnóstico. Do not rewrite test/design_system/goldens/. Do not edit service tests or pubspec.lock-old. Do not copy gallery onPressed: () {}.</action>
  <acceptance_criteria>
    - network_calculator_screen_test.dart is GREEN.
    - AppShell happy path calculates 192.168.1.0 and preserves it after Armazenamento → Rede.
    - lib/screen/network_calculator_screen.dart contains Calcular rede and ToolScaffold and does not contain return Scaffold( or Text('Calcular') or AppBar(.
    - DataConverterScreen and HashGeneratorScreen files are unmodified in this plan.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/network_calculator_screen_test.dart test/app/app_shell_test.dart test/screen/tools_preservation_test.dart test/network_calculator_test.dart test/data_converter_test.dart test/hash_calculator_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>PRES-01 holds with the new chrome; PRES-04 holds for the two unmigrated screens (D-04, D-10, D-13).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm Rede grep gates and unmigrated screens still work</name>
  <files>lib/screen/network_calculator_screen.dart</files>
  <read_first>
    - lib/screen/network_calculator_screen.dart — post-migration
    - lib/screen/data_converter_screen.dart — must still have Analisar Capacidade and Scaffold
    - lib/screen/hash_generator_screen.dart — must still have Text('Calcular') and Clipboard.setData
  </read_first>
  <action>Run the grep gates below and the service+AppShell suite. Isolated Calcular is forbidden on the migrated Rede screen (D-07) but Hash may still contain Text('Calcular') until 02-04. Confirm data_converter_screen.dart still contains Analisar Capacidade and hash_generator_screen.dart still contains Gerador de Hash so PRES-04 is not a vacant claim. Do not start Hash copy migration. Do not rewrite goldens. If a gate fails, fix only NetworkCalculatorScreen or the tests this plan owns.</action>
  <acceptance_criteria>
    - lib/screen/network_calculator_screen.dart has no Scaffold(, no AppBar(, no Text('Calcular'), no onCancel:.
    - lib/screen/data_converter_screen.dart and lib/screen/hash_generator_screen.dart still exist with their pre-migration CTAs.
    - analyze --no-pub is clean.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "return Scaffold\(|AppBar\(|Text\('Calcular'\)|onCancel:" lib/screen/network_calculator_screen.dart; if ($LASTEXITCODE -eq 0) { throw 'Rede still has forbidden chrome/CTA' }
rg -n "Analisar Capacidade" lib/screen/data_converter_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'PRES-04: Armazenamento CTA missing' }
rg -n "Text\('Calcular'\)" lib/screen/hash_generator_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'PRES-04: Hash CTA missing until 02-04' }
C:\src\flutter\bin\flutter.bat test --no-pub test/network_calculator_test.dart test/data_converter_test.dart test/hash_calculator_test.dart test/app/app_shell_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>Rede migration is mechanically clean; the other two tools remain present and callable (PRES-04).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `lib/screen/network_calculator_screen.dart` | ToolScaffold wrap; optional `copyWriter`; CTA `Calcular rede` |
| `test/screen/network_calculator_screen_test.dart` | PRES-01 widget regression |
| `test/app/app_shell_test.dart` | Happy path finders aligned with split rows |
| `test/screen/tools_preservation_test.dart` | Rede group updated; other tools still old chrome |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form text → NetworkCalculator | Untrusted IP/mask/CIDR strings; validators must stay character-exact. |
| TechnicalValueRow → clipboard | Copied text must be the computed address/range, not a spoofed prefix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | _calculate validators | mitigate | Keep isValidIp / empty mask / isValidSubnetMask / isValidCidr error strings exact; do not weaken or move validation into the widget tree (D-09, ASVS V5). |
| T-02-05 | Information Disclosure | TechnicalValueRow copy | mitigate | CopyValueAction copies the displayed value only; inject FakeCopyWriter in tests; no analytics (D-08, D-11). |
| T-02-06 | Elevation of Privilege | CTA / onCancel | mitigate | Limpar is secondary TextButton labeled Limpar; never ToolActionGroup.onCancel (would paint Cancelar and look like a diagnostic cancel). |
| T-02-07 | Denial of Service | Nested Scaffold | mitigate | Delete inner Scaffold/AppBar so SnackBar uses MaterialApp ScaffoldMessenger (D-06). |
| T-02-SC | Tampering | package supply chain | accept | No new packages (D-11). |
</threat_model>

<verification>
1. Widget tests cover happy path 192.168.1.0, four field errors, and copy payload.
2. AppShell happy path and IndexedStack preservation stay green with Calcular rede.
3. Service oracles unchanged. analyze --no-pub clean.
4. Armazenamento and Hash source still have pre-migration chrome.
</verification>

<success_criteria>
- User calculates IPv4 with the same valid results after the visual wrap (PRES-01).
- User still reaches Armazenamento and Hash (PRES-04).
- Isolated Calcular is gone from Rede. Cancelar is never shown as Limpar.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-02-SUMMARY.md` when done
</output>
