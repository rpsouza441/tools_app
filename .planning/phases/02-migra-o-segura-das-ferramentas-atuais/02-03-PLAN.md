---
phase: 02-migra-o-segura-das-ferramentas-atuais
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - lib/screen/data_converter_screen.dart
  - test/screen/data_converter_screen_test.dart
  - test/screen/tools_preservation_test.dart
autonomous: true
requirements:
  - PRES-02
  - PRES-04
must_haves:
  truths:
    - "O usuário analisa 1 TB e vê os mesmos valores de advertised/real/diferença formatados com NumberFormat('#,##0.000', 'pt_BR') (PRES-02, D-09)."
    - "O CTA é Analisar capacidade (sentence case); Analisar Capacidade desaparece da tela (D-07)."
    - "A diferença usa colorScheme.error, nunca Colors.red; não há spinner-only no botão (D-06)."
    - "A cópia educativa Por que a capacidade parece menor? e 931 GiB permanece (PRES-04)."
    - "Rede migrada e Hash ainda não migrado continuam utilizáveis (PRES-04, D-04)."
  artifacts:
    - path: "lib/screen/data_converter_screen.dart"
      provides: "DataConverterScreen em ToolScaffold"
      contains: "Analisar capacidade"
    - path: "test/screen/data_converter_screen_test.dart"
      provides: "Happy path TB, erros de campo e copy educativa"
  key_links:
    - from: "lib/screen/data_converter_screen.dart"
      to: "lib/service/data_converter.dart"
      via: "DataConverter.analyze e availableUnits"
      pattern: "DataConverter\\.analyze"
    - from: "lib/screen/data_converter_screen.dart"
      to: "package:intl/intl.dart"
      via: "NumberFormat('#,##0.000', 'pt_BR') permanece na UI"
      pattern: "NumberFormat\\('#,##0.000', 'pt_BR'\\)"
---

## Phase Goal

**As a** profissional ou estudante de TI, **I want to** continuar resolvendo as mesmas tarefas nas três ferramentas existentes depois da migração para a nova fundação visual, **so that** eu não perco cálculos IPv4, conversão de armazenamento nem hashes.

<objective>
Migrar somente DataConverterScreen para ToolScaffold/seções/TechnicalValueRow, CTA Analisar capacidade, colorScheme.error e NumberFormat pt_BR intacto, sem tocar DataConverter nem a tela Hash (PRES-02, PRES-04, D-03, D-05, D-06, D-07, D-08, D-09, D-14).

Purpose: Segunda fatia vertical. Conversão decimal/binária e o texto educativo são funcionalidade existente (PRES-04), não features novas.
Output: Tela Armazenamento na fundação, widget tests, Rede e Hash ainda operáveis.
</objective>

<execution_context>
@C:/Users/Rodrigo/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-CONTEXT.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-RESEARCH.md
@.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-PATTERNS.md
@lib/screen/data_converter_screen.dart
@lib/model/analysis_result.dart
@lib/service/data_converter.dart
@test/data_converter_test.dart
@test/screen/screen_test_harness.dart
@lib/design_system/tool_scaffold.dart
@lib/design_system/tool_sections.dart

<interfaces>
Keep const DataConverterScreen() for app_destinations _buildDataConverter. Optional copyWriter seam matching Rede/Hash:

- const DataConverterScreen({super.key, this.copyWriter}); final CopyValueWriter? copyWriter;

ToolScaffold title must be Conversor de Armazenamento (current AppBar). Do NOT rename to catalog semanticLabel Conversor de Dados (D-03, PRES-04). Do not edit app_destinations.dart.

Keep in the screen (D-09):
- NumberFormat('#,##0.000', 'pt_BR') and _formatNumber — do not move formatting into DataConverter
- _fromUnit = 'GB', DataConverter.availableUnits, DropdownButtonFormField.initialValue
- errorText strings: Insira um valor ; Insira um valor numérico positivo ; e.toString().replaceAll('ArgumentError: ', '')
- Explanation words: Por que a capacidade parece menor? ; O Padrão do Fabricante (Base 10) ; O Padrão do Sistema (Base 2) ; 1 TB ; 931 GiB ; manufacturer/system ListTile subtitles already in the file

Delete:
- inner Scaffold/AppBar
- input Card + rigid Row value/dropdown
- bool _isLoading, onPressed: _isLoading ? null : _calculate, CircularProgressIndicator as the only button child
- Colors.red on the difference span
- BorderRadius.circular(12) on the explanation card
- CTA Analisar Capacidade

Replace chrome:
- ToolInputSection column: TextField label Valor Anunciado (keep onSubmitted: _calculate) then DropdownButtonFormField label Unidade
- ToolActionGroup primary ElevatedButton(onPressed: _calculate, child: Text('Analisar capacidade')) — no secondary Limpar (screen has none today); never onCancel
- if _result != null: ToolResultCard with TechnicalValueRow / ToolMetric for advertised/real/difference and conversion lines; copied payload = _formatNumber(data.value) + ' ' + data.unit (and the same for advertised/real/difference)
- Difference text style uses Theme.of(context).colorScheme.error, never Colors.red
- Second ToolResultCard wrapping the same explanation copy without local radius 12

Do not add ToolStatusPanel.loading/success/offline. _calculate stays synchronous inside one setState.

Service oracle (do not edit test/data_converter_test.dart): DataConverter.analyze(1, 'TB') → realUnit TiB, realValue closeTo 0.9094947017, difference closeTo 0.0905052982. Widget tests should format with the same NumberFormat('#,##0.000', 'pt_BR') to assert displayed strings.

Relative imports ../service/ and ../model/ may stay; new design-system imports use package:tools_app/design_system/....
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Armazenamento widget tests</name>
  <files>test/screen/data_converter_screen_test.dart</files>
  <read_first>
    - test/screen/screen_test_harness.dart — wrapScreen / FakeCopyWriter
    - lib/screen/data_converter_screen.dart — labels, errors, explanation copy, _formatNumber
    - test/data_converter_test.dart — 1 TB oracle (do not edit)
    - lib/service/data_converter.dart — availableUnits includes TB
  </read_first>
  <behavior>
    - wrapScreen(const DataConverterScreen()): find.byType(AppBar) findsNothing; ToolScaffold present; title Conversor de Armazenamento; find.text('Conversor de Dados') is not the page title (catalog tooltip may exist only inside App(), not this isolated pump).
    - Empty submit (tap Analisar capacidade): Insira um valor.
    - Enter abc, tap Analisar capacidade: Insira um valor numérico positivo.
    - Enter 1, open Unidade dropdown, select TB, tap Analisar capacidade: Analisar Capacidade findsNothing; Analisar capacidade findsWidgets; formatted advertised/real/difference using NumberFormat('#,##0.000', 'pt_BR') appear; real unit TiB appears; find.text('Insira um valor') findsNothing.
    - Explanation always visible: Por que a capacidade parece menor? and 931 GiB.
    - No CircularProgressIndicator as the only child of the primary ElevatedButton.
    - Optional: FakeCopyWriter on a technical row copies the formatted number + unit as displayed.
  </behavior>
  <action>Create data_converter_screen_test.dart with wrapScreen. Tests must be RED because Analisar capacidade and ToolScaffold are not in the screen yet. Do not edit test/data_converter_test.dart. Do not assert only widget types without the TB oracle and error strings (D-13). Selecting TB: tap the dropdown showing GB then tap TB in the menu. Use the same NumberFormat pattern in the test to build expected pt_BR strings rather than hard-coding a guessed grouping that might disagree with intl.</action>
  <acceptance_criteria>
    - RED for missing sentence-case CTA and ToolScaffold, not for a broken dropdown finder.
    - Empty and non-numeric errors are character-exact.
    - Explanation copy is asserted so PRES-04 cannot drop it.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/data_converter_screen_test.dart; if ($LASTEXITCODE -eq 0) { throw 'Expected RED before Armazenamento ToolScaffold migration' }</automated>
  </verify>
  <done>Failing tests lock PRES-02 values, D-07 CTA, and explanation copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap DataConverterScreen without fake loading or Colors.red</name>
  <files>lib/screen/data_converter_screen.dart, test/screen/tools_preservation_test.dart</files>
  <read_first>
    - test/screen/data_converter_screen_test.dart — RED contract
    - lib/screen/data_converter_screen.dart — _calculate, _formatNumber, _buildResultCard, _buildExplanationCard
    - lib/design_system/tool_status_panel.dart — colorScheme.error analog for failure color
    - test/screen/tools_preservation_test.dart — Armazenamento group still expects Analisar Capacidade and AppBar
  </read_first>
  <action>Rewrite only the chrome of DataConverterScreen per interfaces (D-05, D-06, D-07, D-08). Delete _isLoading entirely; never introduce ToolStatusPanel.loading (D-06, pitfall 7). Replace Colors.red with Theme.of(context).colorScheme.error on the difference span. Keep NumberFormat in the UI and DataConverter.analyze as the only calculation (D-09). Keep explanation wording including 931 GiB. Optional copyWriter must not break const DataConverterScreen(). Update tools_preservation_test Armazenamento group: heading Conversor de Armazenamento (not AppBar), CTA Analisar capacidade; Rede group stays Calcular rede; Hash group still expects AppBar Gerador de Hash and Calcular (PRES-04, D-04). Do not edit network_calculator_screen.dart, hash_generator_screen.dart, app_destinations.dart, services, goldens, or pubspec.lock-old. Do not add a Limpar button that did not exist. Do not use onCancel.</action>
  <acceptance_criteria>
    - data_converter_screen_test.dart GREEN.
    - File contains Analisar capacidade and ToolScaffold; does not contain Analisar Capacidade, Colors.red, _isLoading, or return Scaffold(.
    - NumberFormat('#,##0.000', 'pt_BR') still lives in the screen.
    - HashGeneratorScreen is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>C:\src\flutter\bin\flutter.bat test --no-pub test/screen/data_converter_screen_test.dart test/screen/tools_preservation_test.dart test/screen/network_calculator_screen_test.dart test/app/app_shell_test.dart test/network_calculator_test.dart test/data_converter_test.dart test/hash_calculator_test.dart
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>PRES-02 holds; Rede remains on ToolScaffold; Hash still has old chrome and works (PRES-04).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm Armazenamento grep gates and Hash still present</name>
  <files>lib/screen/data_converter_screen.dart</files>
  <read_first>
    - lib/screen/data_converter_screen.dart
    - lib/screen/hash_generator_screen.dart — still Clipboard.setData / Calcular
    - lib/screen/network_calculator_screen.dart — still Calcular rede
  </read_first>
  <action>Grep-confirm the migrated screen has no Colors.red, no _isLoading, no inner Scaffold/AppBar, no Analisar Capacidade, no onCancel. Confirm Hash still has Text('Calcular') and Rede still has Calcular rede so this increment did not silently migrate a second production screen (D-04, D-14). Do not start Hash. Do not rewrite goldens.</action>
  <acceptance_criteria>
    - data_converter_screen.dart has none of the forbidden tokens listed in verify.
    - hash_generator_screen.dart still contains Clipboard.setData and Text('Calcular').
    - network_calculator_screen.dart still contains Calcular rede.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Colors\.red|_isLoading|return Scaffold\(|AppBar\(|Analisar Capacidade|onCancel:" lib/screen/data_converter_screen.dart; if ($LASTEXITCODE -eq 0) { throw 'Armazenamento still has forbidden chrome' }
rg -n "Clipboard.setData" lib/screen/hash_generator_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'PRES-04: Hash copy path missing until 02-04' }
rg -n "Calcular rede" lib/screen/network_calculator_screen.dart; if ($LASTEXITCODE -ne 0) { throw 'PRES-04: Rede CTA missing' }
C:\src\flutter\bin\flutter.bat analyze --no-pub</automated>
  </verify>
  <done>Armazenamento chrome is clean; Hash remains the only unmigrated production screen (PRES-04).</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New public symbols / role |
|---|---|
| `lib/screen/data_converter_screen.dart` | ToolScaffold wrap; CTA `Analisar capacidade`; `colorScheme.error` |
| `test/screen/data_converter_screen_test.dart` | PRES-02 widget regression |
| `test/screen/tools_preservation_test.dart` | Armazenamento group updated |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TextField value → DataConverter.analyze | Untrusted numeric string; keep empty/non-positive errors on the field. |
| Formatted numbers → clipboard | Copied text is the displayed pt_BR string plus unit, not a raw double. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | _calculate parse path | mitigate | Keep Insira um valor and Insira um valor numérico positivo; still use double.tryParse after comma-to-dot; do not call analyze with empty/non-positive input (D-09, ASVS V5). |
| T-02-09 | Spoofing | difference color | mitigate | Use colorScheme.error so dark/light themes keep meaning; never Colors.red (D-06, D-11). |
| T-02-10 | Denial of Service | fake _isLoading spinner | mitigate | Delete _isLoading; primary button always labeled Analisar capacidade (UI-SPEC loading rule). |
| T-02-SC | Tampering | package supply chain | accept | intl already pinned; do not flutter pub add (D-11). |
</threat_model>

<verification>
1. Widget tests cover empty, non-numeric, 1 TB formatted results, and explanation copy.
2. AppShell happy path (Rede) still green. Service oracles untouched.
3. Hash still on old chrome. analyze --no-pub clean.
</verification>

<success_criteria>
- User converts storage with the same valid results after the wrap (PRES-02).
- User still uses Rede (migrated) and Hash (not yet migrated) (PRES-04).
- No Colors.red, no fake loading, no catalog title swap to Conversor de Dados.
</success_criteria>

<output>
Create `.planning/phases/02-migra-o-segura-das-ferramentas-atuais/02-03-SUMMARY.md` when done
</output>
